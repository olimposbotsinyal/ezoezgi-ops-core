# BACKLOG — EzoEzgi Ops

> PLAN.md içindeki aktif sprint kapsamı dışında kalan tüm istekler buraya yazılır.
> Sprint ortasında PLAN.md kapsamı değişmez; yeni fikir/istek önce buraya düşer.

| ID | Başlık | Öncelik | Durum | Faz | Sorumlu Ajan | Not |
|---|---|---|---|---|---|---|
| B001 | Kök `README.md` + proje özeti | Yüksek | Açık | Faz 0 | — | PLAN.md T3 ile aynı, referans amaçlı |
| B002 | Config loader — hot reload | Yüksek | Açık | Faz 0 | Orchestrator | PLAN.md T5 |
| B003 | Ollama local runtime kurulum betiği | Yüksek | Açık | Faz 1 | Bridge Agent | Model indirme + sağlık kontrolü |
| B004 | Whisper STT entegrasyonu | Orta | Açık | Faz 3 | Voice Agent | services/stt-whisper iskeleti boş |
| B005 | TTS servis seçimi (local model kararı) | Orta | Açık | Faz 3 | Voice Agent | DECISIONS.md'de ADR gerekiyor |
| B006 | Finance Engine veri modeli tasarımı | Orta | Açık | Faz 2 | Finance Agent | Harcama/gelir şeması |
| B007 | Social Engine onay akışı (auto-post guard) | Yüksek | Açık | Faz 5 | Social Agent | §8 güvenlik mekanizmasına bağımlı |
| B008 | Research Engine — web erişim politikası | Orta | Açık | Faz 2 | Research Agent | Offline/online fallback davranışı |
| B009 | Doc Ingestion — desteklenecek dosya formatları | Düşük | Açık | Faz 2 | Doc Agent | PDF/DOCX/TXT önceliklendirme |
| B010 | GSM Gateway — sağlayıcı/donanım seçimi | Orta | Açık | Faz 3 | Device Agent | SIM/modem tipi netleşmeli |
| B011 | Bluetooth cihaz senaryoları | Düşük | Açık | Faz 3+ | Device Agent | Kapsam MASTER_ROADMAP §4'te ertelendi |
| B012 | Gesture-vision — privacy-by-default doğrulama | Yüksek | Açık | Faz 5 | Device Agent | Görüntü diske yazılmama garantisi |
| B013 | Admin Panel — ilk wireframe | Düşük | Açık | Faz 5 | — | apps/admin-panel şu an boş |
| B014 | Mobile PWA — offline cache stratejisi | Orta | Açık | Faz 3 | — | Service worker tasarımı |
| B015 | Prometheus/Grafana dashboard seti | Düşük | Açık | Faz 6 | — | infra/monitoring iskeleti boş |
| B016 | OSV taramasının CI'ya bağlanması | Orta | Açık | Faz 4 | — | CI henüz kurulu değil |
| B017 | Audit log şeması (append-only) | Yüksek | Açık | Faz 4 | Orchestrator | data/audit formatı netleşmeli |
| B018 | Risk seviyesi taksonomisi detaylandırma | Yüksek | Açık | Faz 4 | Orchestrator | low/medium/high/irreversible kriterleri; T13'te ilk çalışan sürüm (`risk_engine.py` + `tool_risk_policy.yaml`) teslim edildi, Faz 4'te daha ayrıntılı kriterler/hardening bekliyor |
| B019 | Çok dilli genişleme (TR/EN dışı) | Düşük | Açık | Backlog | Bridge Agent | Şimdilik kapsam dışı, ADR gerektirir |
| B020 | Native mobil uygulama ihtiyacı değerlendirmesi | Düşük | Açık | Backlog | — | PWA yeterli mi — ayrı ADR |
| B021 | Exchange connector abstraction (Binance/Bybit/BtcTurk vb.) | Yüksek | Açık | Faz 2 | Finance Agent | Ortak arayüz, borsa-özel adaptörler; gerçek bağlantı bu maddede yok |
| B022 | Order sizing policy | Yüksek | Açık | Faz 2 | Finance Agent | L2/L3 eşiklerini belirleyen pozisyon büyüklüğü kuralları; policies/risk/finance.yaml ile ilişkili |
| B023 | Stop-loss / take-profit policy | Yüksek | Açık | Faz 2 | Finance Agent | Otomatik risk sınırlama kuralları; execution mock (PLAN.md T19) sonrası netleşecek |
| B024 | Circuit breaker (max günlük zarar) | Yüksek | Açık | Faz 2 | Finance Agent | Eşik aşılınca tüm L2/L3 işlemleri otomatik durdurma; RUNBOOK kill switch ile ilişkili |
| B025 | API permission validator (withdraw=false kontrolü) | Yüksek | Açık | Faz 2 | Finance Agent | Bağlantı kurulmadan önce API key izinlerini doğrular; MASTER_ROADMAP.md §5 zorunlu ön koşul |
| B026 | Gelisen_Bot parity checklist | Yüksek | Açık | Faz 2 | Finance Agent | `docs/imports/GELISEN_BOT_PARITY_CHECKLIST.md`; T19'un yeniden açılması için ön koşul (ADR-011) |
| B027 | Telegram command adapter tasarımı | Orta | Açık | Faz 2 | Bridge Agent | Gelisen_Bot'un ikili Telegram kütüphanesi (python-telegram-bot + telebot) karmaşasından kaçınacak tek-kütüphane adapter; `GELISEN_BOT_ANALIZ_RAPORU.md` §3 referans |
| B028 | Strategy abstraction katmanı | Orta | Açık | Faz 2 | Finance Agent | Gelisen_Bot `strategies/base_strategy.py`/`strategy_v1.py` deseninden ilham; EzoEzgi Finance Agent'a uyarlanacak |
| B029 | Dry-run replay test altyapısı | Orta | Açık | Faz 2 | Finance Agent | Geçmiş piyasa verisiyle strateji/risk motorunu gerçek para olmadan (L1) tekrar oynatma; Gelisen_Bot'ta karşılığı yok, yeni yetenek |
| B030 | Kullanıcı borsa credential encryption | Yüksek | Açık | Faz 2 | Finance Agent | Gelisen_Bot'ta plaintext DB saklama tespit edildi (`GELISEN_BOT_SECURITY_NOTES.md` P1); EzoEzgi'de envelope encryption zorunlu, B021 öncesi ön koşul |
| B031 | Gerçek TR-EN çeviri/NLU entegrasyonu | Yüksek | **Kısmen tamamlandı — Ollama runtime çöküyor (llama-server crash), yeni engel** | Faz 1 | Bridge Agent | Adaptör + bayrak + quality gate (golden set + `tools/eval_nlu.py` + eşikler) tamam. **2026-08-13T00:01:19Z:** ilk canlı ölçüm — 5/5'ten 4 FAIL, kök neden: istemci timeout'u (2.0s) gerçek inference'tan (~6s) kısaydı. **Düzeltildi** (`model_client.py::DEFAULT_TIMEOUT_SECONDS`→30.0s + `OLLAMA_TIMEOUT_SECONDS` env override, 6 yeni test, 142/142 regresyon yeşil). **2026-08-13T00:13:05Z: ikinci canlı ölçüm** (`reports/nlu_eval_20260813.md`) — timeout düzeltmesi doğrulandı çalışıyor (istekler artık kesilmiyor, gerçek sunucuya ulaşıyor, p95 12.89s) ama **yeni bir engelle karşılaşıldı**: Ollama'nın `llama-server` alt süreci her istekte çöküyor (`HTTP 500`, `"llama-server process has terminated: exit status 0xc0000005"` — Windows access violation; `curl` ile manuel doğrulandı). RAM yeterli (66.9GB/23GB boş) — kaynak kısıtı değil, muhtemelen Ollama/llama.cpp build'i ile bu makine arasında bir uyumsuzluk/bug. Sonuçlar sayısal olarak ilk koşuyla aynı (%30/%0/%100) ama nedeni farklı. B031 hâlâ **Partial / FAILED_THRESHOLDS**. Sonraki adım netleştirilmeyi bekliyor: farklı/daha küçük bir model denemek (ör. `phi3`, `llama3.2:1b`), Ollama'yı güncellemek/yeniden kurmak, veya crash'i ayrıca teşhis etmek |
| B032 | Python bağımlılık dosyası (dev dependencies) | Orta | Açık | Faz 0/1 | — | `pytest` + `pyyaml` şu an yalnızca `.venv`'e ad-hoc kuruldu; reproducibility için `pyproject.toml` `[project.dependencies]` veya `requirements-dev.txt` eklenmeli |
| B033 | Alias matcher'da Türkçe ek toleransı | Düşük | Açık | Backlog | Bridge Agent | `alias_matcher.py` yalnızca tam kelime eşleşmesi yapıyor; "ezgi'ye" gibi ek almış formlar kapsam dışı bırakıldı (bkz. `tests/test_alias_matcher.py` "EZGİYE söyle" negatif vakası) |
| B034 | `cli-runner` genel whitelist mekanizması (T12) | Yüksek | **Kapalı** | Faz 0 (Gün 10-11) | — | 2026-08-13'te T12 ile tamamlandı: `tools/cli-runner/src/runner.py` + `config/cli_whitelist.json` (bkz. ADR-012); `echo_runner.py` artık bunu kullanıyor |
| B035 | Risk taksonomisi uyumlulaştırma (genel vs finans) | Orta | Açık | Faz 2 | Orchestrator | T13'ün genel `low/medium/high/irreversible` şeması (`tool_risk_policy.yaml`) ile finansın `L0-L3` şeması (T18, MASTER_ROADMAP.md §5) şu an paralel/ayrı; Faz 2'de finans task'ları genel registry'ye eklenirken iki şemanın nasıl ilişkileneceği netleşmeli |
| B036 | Ollama Windows `0xc0000005` (`llama-server` crash) araştırılması | **Yüksek** | Açık — **kök neden log-kanıtlı izole edildi (GPU/Vulkan), kalıcı çözüm bekliyor** | Faz 1 | Bridge Agent | 2026-08-13T00:38:34Z stabilizasyon kapısı (`reports/runtime_diag_20260813T003834Z/`): 4/4 tekli test aynı `0xc0000005` ile çöktü, `OLLAMA_NUM_GPU=0` yetersiz kaldı (Vulkan cihazı log'da hâlâ görünüyordu). **2026-08-13T01:00Z derin triage** (`reports/runtime_incident_20260813T004855Z/`): Host fingerprint — NVIDIA sürücü **442.94/CUDA 10.2** (Ollama'nın `cuda_v12/v13` beklentisinden çok eski) + Vulkan Instance **1.2.131** (eski) + Event Viewer'da 20 özdeş çöküş kaydı (`Hatalı modül: unknown`). **GPU/Vulkan izolasyon matrisi (A-E):** `OLLAMA_VULKAN=false` VE `OLLAMA_LLM_LIBRARY=cpu` — ikisi de sunucu loglarıyla **kanıtlanmış** şekilde Vulkan cihazını hiç başlatmıyor (`library=cpu` seçiliyor, `Vulkan`/`Quadro RTX 3000` log'da hiç geçmiyor) ve bu modda **5/5 çağrı başarılı, 0 çöküş**. Vulkan cihazı başlatıldığında (baseline) ise **5/5 çöküş**. **Sürüm A/B:** mevcut `0.32.9` çöküyor; önceki `0.30.0` (gerçekten kurulup test edildi, sonra `0.32.9`'a geri yüklendi) varsayılan ayarlarla çökmüyor — ama log'a göre bu sürüm de Vulkan cihazını bu donanımda hiç seçmiyor (otomatik CPU'ya düşüyor), yani "0.30.0'da düzeltilmiş" değil, farklı GPU-keşif davranışı. **Sonuç: GPU/Vulkan sürücü etkileşimi kök neden olarak kesin izole edildi (log kanıtlı).** Reprodüksiyon scripti: `scripts/repro_ollama_crash.ps1`. Upstream issue taslağı hazır: `reports/runtime_incident_20260813T004855Z/OLLAMA_GITHUB_ISSUE_DRAFT.md` (durum: **AÇILDI**, bkz. aşağıdaki güncelleme). **Kabul kriteri (değişmedi):** B031 quality gate'i yeniden koşmadan önce **varsayılan (Vulkan etkin) ortamda** 50/50 başarılı, 0 HTTP 500, 0 `0xc0000005`. CPU-only workaround (`OLLAMA_VULKAN=false`) çökmüyor ama gecikmesi (7.7s-26.9s/çağrı) B031'in `latency_p95≤2.5s` eşiğini muhtemelen geçemez. **Sonraki adımlar:** (1) NVIDIA sürücüsünü güncellemek (442.94 → güncel) ve Vulkan modunda yeniden test etmek, (2) upstream issue'yu göndermek, (3) sürücü güncellemesi çözmezse B031'in gecikme eşiğini CPU-only gerçeğine göre ayrı bir ADR ile yeniden değerlendirmek. **2026-08-13T01:19Z post-triage aksiyonlar:** Upstream issue bu ortamdan açılamamıştı (gh CLI/token yok) — gönderime hazır tam paket `reports/runtime_incident_20260813T004855Z/ISSUE_READY_PACKAGE.md` hazırlandı. **2026-08-13 ~05:02 (+03:00) güncelleme: Upstream issue durumu READY_TO_SUBMIT → AÇILDI.** Kullanıcı paketi kullanarak manuel gönderdi: <https://github.com/ollama/ollama/issues/17716> (bkz. `docs/RUNBOOK.md` "B036 Upstream Issue — Durum"). B036 genel durumu **IN_PROGRESS** olarak kalıyor (issue açılması B036'yı çözmüyor, yalnızca upstream takibini başlatıyor). Geçici CPU-only workaround formalize edildi: `docs/ops/OLLAMA_WORKAROUND_CPU_ONLY.md` (`OLLAMA_VULKAN=false`/`OLLAMA_LLM_LIBRARY=cpu`) — yalnızca bilgilendirici koşular için, resmi B031 kararını etkilemez. **B036 çıkış kriterleri (resmi, değişmez):** (1) varsayılan (Vulkan etkin, workaround YOK) profilde 50/50 başarılı `/api/generate` çağrısı, (2) 0 `HTTP 500`, (3) loglarda 0 `0xc0000005`, (4) yalnızca bunlar sağlandıktan sonra B031 resmi gate'i yeniden koşulur. **Sürücü güncelleme takip maddesi (açık, sonuç bekliyor):** hedef — NVIDIA Quadro RTX 3000 sürücüsünü `442.94`'ten güncel bir sürüme yükseltmek (hedef sürüm/pin henüz seçilmedi, sonraki turda netleştirilecek); sonuç bu satıra ve `docs/RUNBOOK.md`'ye loglanacak. **2026-08-13T02:33Z Post-upstream Experiment Batch #1** (`reports/runtime_incident_20260813T004855Z/exp_01_post_upstream/`, n=100, 5 senaryo × 20 çağrı, script: `scripts/repro_b036_batch.ps1`): A (Vulkan ON baseline) 20/20 çöküş; B (CPU-only) 20/20 başarı; C (`OLLAMA_NUM_PARALLEL=1`) 20/20 çöküş; D (`num_ctx=256`) 20/20 çöküş; E (her çağrıda temiz restart) 20/20 çöküş. **Toplam 80/100 çöküş — tüm Vulkan-etkin varyasyonlarda %0 başarı, istisnasız; yalnızca CPU-only crash-free.** Önceki n=5 bulgusu n=20/senaryo ile tam doğrulandı. **B036 kararı: IN_PROGRESS (READY_FOR_RETEST'e geçilmedi — hiçbir Vulkan-etkin senaryo crash-free değil).** B031 gate atlandı, neden: `blocked_by_runtime_instability`. Upstream takip yorumu taslağı hazırlandı: `reports/runtime_incident_20260813T004855Z/exp_01_post_upstream/upstream_comment_draft_17716.md` (henüz gönderilmedi, manuel gönderim bekliyor — bkz. `docs/RUNBOOK.md` "Upstream Follow-up Prepared"). **2026-08-13 ~05:47 (+03:00): Watch mode active.** Takip yorumu kullanıcı tarafından issue #17716'ya manuel eklendi (permalink: <https://github.com/ollama/ollama/issues/17716#issuecomment-5275320830>). Maintainer yanıtı gelene kadar geniş kapsamlı yeni deney partisi çalıştırılmayacak — protokol: `docs/ops/B036_UPSTREAM_WATCH_PROTOCOL.md`, kontrol günlüğü: `reports/runtime_incident_20260813T004855Z/upstream_watch_log.md`, tetikleyici bazlı doğrulama planı: `reports/runtime_incident_20260813T004855Z/validation_on_trigger.md`. **B036 durumu değişmedi: IN_PROGRESS.** Watch checks are automated via `scripts/watch_b036_upstream.py`. **Resilience layer added:** `services/model-gateway/src/model_gateway/` — sıralı, açık-loglu çok-sağlayıcılı fallback (ollama→local_alt→remote, varsayılan yalnızca ollama, remote politika kapılı). B036'yı çözmez, yalnızca tek sağlayıcı çökmesinin sistemi tamamen durdurmasını önler; bkz. `docs/ops/MODEL_FALLBACK_RUNBOOK.md`. **CPU-only runtime verification guard eklendi** (`runtime_verify.py`): `OLLAMA_VULKAN=false` zorlamasının yalnızca best-effort (harici sürece etkisiz) olması riskini kapatmak için, Ollama birincil olarak seçilmeden önce kanıt-temelli bir doğrulama kapısı çalışır — operatör marker dosyası (pozitif kanıt), Windows Event Viewer'da bilinen çöküş imzası taraması (negatif kanıt), `/api/ps` VRAM kontrolü (zayıf negatif kanıt). **ÖNEMLİ:** `OLLAMA_CPU_VERIFY_STRICT=true` varsayılanıyla, operatör marker dosyası oluşturmadıkça doğrulama genellikle `UNKNOWN` döner ve Ollama STRICT modda **birincil olarak seçilmez** — bu, kasıtlı ve `docs/ops/MODEL_FALLBACK_RUNBOOK.md`'de yüksek sesle belgelenmiş bir tasarım kararıdır, "sessiz davranış değişikliği yok" ilkesi gereği burada de not düşülüyor. Yeni reason code'lar: `PRIMARY_RESTRICTED_CPU_UNVERIFIED`, `FALLBACK_EXHAUSTED`. Yeni audit event'leri: `OLLAMA_CPU_PREFLIGHT_CHECKED`, `OLLAMA_PRIMARY_RESTRICTED` (tümü ortak bir `trace_id` ile korelasyonlu). **SLI/SLO/alert/ops otomasyonu eklendi:** `services/model-gateway/src/model_gateway/metrics.py` (bellek-içi sayaç/histogram/gauge, canlı Prometheus endpoint'i yok — bkz. `docs/BACKLOG.md` B015), `docs/ops/SLO_MODEL_GATEWAY.md` (Availability/Degradation/Primary-Usage/Preflight-Confidence SLI'ları, burn-rate örnekleri), `docs/ops/ALERT_PLAYBOOK_MODEL_GATEWAY.md` (5 alert tipi + ilk aksiyonlar, `infra/monitoring/prometheus/model_gateway_alerts.yaml` örnek kural seti — henüz hiçbir canlı Prometheus/Alertmanager tarafından değerlendirilmiyor), `scripts/ops/daily_gateway_smoke.ps1` (günlük sağlık kontrolü — config sanity, preflight/sağlık anlık görüntüsü, yıkıcı-olmayan sentetik classify probu, audit-append doğrulaması, exit code 0/1/2, kanıt paketi `reports/daily_smoke_<UTC>/`), `scripts/ops/package_gateway_incident.ps1` (olay kanıt paketleyici — audit dilimi + maskelenmiş config parmak izi + git SHA/değişen dosyalar, `reports/incidents/gateway_<timestamp>/`). Remote policy gate zayıflatılmadı (`remote_policy_gate=required` sabit kaldı, daily smoke bunu ayrıca kritik bulgu olarak kontrol ediyor). **Canlı metrics + izleme yığını (Commit G/H):** `scripts/ops/serve_metrics.py` — gerçek, test edilmiş `GET /metrics` (stdlib, bağımlılıksız). Keşif ile doğrulandı: bu makinede Docker/Prometheus/Alertmanager/FastAPI **kurulu değil**. `infra/monitoring/prometheus/prometheus.yml` + `infra/monitoring/alertmanager/alertmanager.yml` (Aşama 1/observe-only, `null-receiver`) geçerli ama okunmuyor. `scripts/ops/emit_synthetic_gateway_signals.py` + `scripts/ops/verify_alert_pipeline.ps1` gerçekten çalıştırıldı — bu makinede dürüstçe **exit code 1 (kısmi)** üretti (metrics UP, Prometheus/Alertmanager yok). Geliştirme sırasında bulunan ve düzeltilen gerçek bir hata: sentetik sinyal başlangıçta ayrı bir süreçte enjekte ediliyordu ve `/metrics` çıktısında hiç görünmüyordu (`get_metrics()` süreç-içi singleton) — `serve_metrics.py`'ye `--enable-debug-injection` + `POST /debug/inject_synthetic` eklenerek düzeltildi, elle doğrulandı. Gate A-D (24s gözlem, %99 scrape, sentetik alert doğrulama, regresyon yok) tam sağlanamadığından **warning/critical routing açılmadı**, rollout Aşama 1'de kalıyor. Tam kılavuz: `docs/ops/MONITORING_STACK_RUNBOOK.md`. **2026-08-13 Cross-process metrik sink'i eklendi (Commit I/J):** `/metrics`'in "yalnızca kendi sürecinde biriken metrikleri gösterir" sınırlaması `METRICS_SINK=jsonl_append` (yeni varsayılan) ile kapatıldı — `model_gateway/metrics_sink.py` (`JsonlAppendSink`: kilit + boyut/gün rotasyonu + retention + sınırlı-kardinalite koruması) ve `model_gateway/metrics_aggregate.py` (JSONL'den okuyup tek bir Prometheus görüntüsüne birleştirme, sert okuma hatasında `AggregationError`) eklendi; `serve_metrics.py`, `/metrics` her çağrıldığında bu aggregator'ı kullanacak şekilde güncellendi ve aggregator sert hatasında **503 + tanılama nedeni** döner, ayrıca öz-metrikler (`metrics_sink_write_failures_total`, `metrics_events_dropped_total`, `metrics_aggregator_read_failures_total`) yanıta eklendi. **Gerçek 2 ayrı OS süreci + `serve_metrics.py` üçüncü süreç ile elle doğrulandı** (curl ile gerçek HTTP GET, iki yazıcının toplam katkısı tek counter'da doğru toplandı). Geriye dönük uyumlu: `METRICS_SINK=in_memory` eski davranışı opt-in korur, `classify()` sözleşmesi/fallback mantığı/remote varsayılanları değişmedi. Bilinen ödünleşimler (eventual-consistency penceresi, scrape gecikmesi, öz-metriklerin süreç-yerelliği): `docs/ops/MONITORING_STACK_RUNBOOK.md` "Bilinen ödünleşimler". Tam regresyon 205/205 yeşil. **2026-08-13 Performans donusumu (Commit K/L):** `/metrics`'in her scrape'te tüm JSONL geçmişini yeniden taraması giderildi — `model_gateway/metrics_index.py` (dosya kimligi `(st_dev, st_ino)` uzerinden, rename/rotasyona SEFFAF, kalici state `data/metrics/*.state.json`), `IncrementalAggregator` (`metrics_aggregate.py` — yalnizca yeni byte'lari okur, `compact_to_registry()`'yi DOGRUDAN yeniden kullanarak tam-yeniden-taramayla BIREBIR AYNI cikti uretir, testle kanitlandi) ve `CachedMetricsRenderer` (`metrics_cache.py` — `METRICS_AGG_CACHE_TTL_SEC` icindeki tekrar istekleri onbellekten yanitlar) eklendi. Yeni guardrail'ler: `METRICS_AGG_MAX_SERIES`/`METRICS_AGG_MAX_EVENTS_PER_SCRAPE` (asim durumunda veri kaybi degil, guvenli dusurme/erteleme + oz-metrik: `metrics_aggregator_series_dropped_total`, `metrics_aggregator_events_deferred_total`, ayrica onbellek icin `metrics_aggregator_cache_hits_total`/`_misses_total`/`_rebuild_duration_ms`). Gercek benchmark (`scripts/ops/benchmark_metrics_scrape.py`, 5000 olay/~6 rotasyon dosyasi): p50 scrape gecikmesi tam-yeniden-taramada ~249ms, artimli+onbeleksiz ~45ms (**~5.6x**), artimli+onbellek isabetinde ~0.004ms — ayrintili tablo ve trafik-bazli ayar rehberi: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Performans ayar rehberi". Bozuk/eksik state dosyasi SERT hata degildir, otomatik yeniden insa edilir (auto-heal, testle kanitlandi). Geriye donuk uyumlu: `classify()` sozlesmesi/fallback/remote politika degismedi, `AggregationError`→503 davranisi korundu. B036 durumu degismedi: **IN_PROGRESS** (bu iş gözlemlenebilirlik altyapısını genişletir, B036'nın kendisini çözmez). **2026-08-13 Go-live paketi eklendi (Commit M/N):** `scripts/ops/run_observability_gates.ps1` + `observability_gates_core.py` (otomatik Gate A-E: metrics availability, scrape success, 4 sentetik alert modunun görünürlüğü, Alertmanager alma yolu, classify regresyon smoke — `gate_report.md`+`gate_results.json`, exit code 0/1/2, `overall_exit_code` haritalaması: FAIL varsa 2, PARTIAL/SKIPPED varsa 1, hepsi PASS ise 0). Bu makinede **gerçek elle çalıştırıldı, sonuç FAIL (exit 2)**: A/B/C PASS, D SKIPPED (Alertmanager kurulu değil), E FAIL — kök neden B037 (aşağıya bakın), classify/fallback sözleşmesiyle İLGİSİZ. `scripts/ops/calibrate_alert_thresholds.py` (son N saatlik gerçek/sentetik-hariç veriden WARN/CRIT önerisi + gerekçe + yanlış-pozitif riski notu) — bu makinede gerçek trafik olmadığından `INSUFFICIENT_DATA`, mevcut varsayılanlar korundu (dürüst, fabrike edilmemiş sonuç). `scripts/ops/build_observability_signoff.py` (git SHA + gerçek tam pytest çalıştırması + en son gate sonucu + GO/CONDITIONAL-GO/NO-GO kararı, `SIGNOFF.md`/`SIGNOFF.json`) — gate_results.json yoksa NO-GO döner, ASLA varsayılan GO uydurmaz. `scripts/ops/rollback_observability.ps1` + `rollback_observability_core.py` (varsayılan **dry-run**, `-Apply` ile gerçek uygulama; yalnızca `alertmanager.yml` route receiver'larını metin-tabanlı, hedefli değişiklikle Aşama 1'e döndürür — YAML'i tam yeniden serilestirmez ki aciklama satirlari KORUNSUN; `/metrics`'i ASLA kapatmaz, `data/audit/`+`data/metrics/` verisine ASLA dokunmaz; idempotent). Hem dry-run hem apply, hem "zaten güvenli" hem "gerçek escalate edilmiş durumu düzeltme" (geçici olarak `warning-sink`/`critical-sink`'e alınıp geri alındı, git diff ile doğrulandı, sonra orijinal dosyaya geri döndürüldü) senaryolarında elle test edildi. Kanonik "Go/No-Go checklist": `docs/ops/MONITORING_STACK_RUNBOOK.md`. Tam regresyon 265/265 yeşil (Bash üzerinden). **2026-08-13 NO-GO engelleri kapatıldı (Commit O/P):** Önceki go-live sign-off NO-GO idi (Gate E FAIL — B037, Gate D SKIPPED — Alertmanager kurulu değildi). B037 düzeltildi (bkz. B037 satırı). Gate D için GERÇEK Prometheus v3.13.2 + Alertmanager v0.33.1 indirilip yerel çalıştırıldı (repo'ya commit edilmedi), sentetik `circuit-open-stuck` sinyali gerçekten `firing`'e geçti ve Alertmanager bunu ALDI (`null-receiver`'a yönlendirilerek, observe-only KORUNDU) — kanıt: `reports/gate_d_real_validation_20260813T072946Z/` (`git add -f`). `run_observability_gates.ps1` yeniden çalıştırıldı: **5/5 gate PASS** (`reports/go_live_gates_20260813T073109Z/`). `build_observability_signoff.py` yeniden çalıştırıldı: karar **GO** (`reports/observability_signoff_20260813T073140Z/`, ikisi de `git add -f`). Tam suite 278/278 yeşil hem Bash hem native PowerShell'de. B036 durumu değişmedi: **IN_PROGRESS** (bu iş gözlemlenebilirlik altyapısını genişletir/olgunlaştırır, B036'nın kendisini çözmez). |
| B037 | `tools/cli-runner/src/runner.py` PowerShell PATH'inde `echo` çalıştırılabilir dosyasını bulamıyor | Orta | **Kapalı** | Backlog | — | `shutil.which("echo")`, Git Bash'in PATH'inde gerçek bir `echo.exe` bulur ama saf bir PowerShell sürecinin PATH'inde `echo` yalnızca bir dil anahtar kelimesi/alias'tır, gerçek bir yürütülebilir DEĞİLDİR — bu yüzden `test_nlu_provider_flag.py`'nin 3 orchestrator-smoke alt-testi PowerShell üzerinden çalıştırılınca `EXECUTABLE_NOT_FOUND` ile başarısız olurdu (Bash üzerinden sorunsuz geçerdi). 2026-08-13'te `scripts/ops/run_observability_gates.ps1`'in Gate E'sini gerçek PowerShell'de çalıştırırken keşfedildi. Classify/fallback SÖZLEŞMESİNDE bir regresyon DEĞİLDİ — yalnızca `echo_runner.py`'nin CLI aracı keşif mekanizmasının Windows/PowerShell-özel bir PATH sınırlaması. **2026-08-13 DÜZELTİLDİ (Commit O):** `runner.py`'ye, `shutil.which()` başarısız olduğunda küçük/sabit bir komut kümesi (`echo`, `pwd`) için SAF PYTHON eşdeğerleri kullanan bir fallback eklendi (`cmd.exe /c` gibi bir kabuğa fallback YAPILMADI — bu, "shell=True asla kullanılmaz" güvenlik ilkesini dolaylı olarak ihlal ederdi; saf-Python yol subprocess/shell yüzeyi olmadığından harici bir yürütülebilirden DAHA AZ değil DAHA GÜVENLİDİR). Bilinmeyen komutlar için eski davranış (açık `EXECUTABLE_NOT_FOUND` hatası) aynen korundu. 7 yeni parite testi eklendi (`tests/test_cli_runner.py`) — `shutil.which` hem başarılı hem başarısız senaryoları mock'lanarak, gerçek makine PATH durumundan bağımsız deterministik doğrulandı. Sonuç: tam suite (278 test) hem Git Bash hem native PowerShell'de **278/278 yeşil** (önceden PowerShell'de 3 test başarısızdı). |

## B036 Çıkış Kriterleri (Kilitli Checklist)

> Bu checklist, B036'nın `IN_PROGRESS` → `READY_FOR_RETEST` geçişi için
> **tüm maddeleri birden** sağlaması gereken, kilitlenmiş resmi kriter
> setidir. Hiçbir madde varsayımla/tahminle işaretlenemez — yalnızca
> gerçek, kanıtlanmış bir koşu sonucunda işaretlenebilir. Güncel durum:
> **IN_PROGRESS** (aşağıdaki maddelerin hiçbiri henüz sağlanmadı).

- [ ] Vulkan yolunda (varsayılan profil, workaround YOK) en az **50 ardışık**
  çağrı çökmeden tamamlanmalı
- [ ] Repro scripti (`scripts/repro_ollama_crash.ps1` veya
  `scripts/repro_b036_batch.ps1`), **2 ayrı soğuk restart** arasında
  tutarlı şekilde başarı üretmeli
- [ ] stderr/log paketinde **`0xc0000005` imzası hiç görünmemeli**
- [ ] Kanıt, incident report yolu altında arşivlenmeli
  (`reports/runtime_incident_20260813T004855Z/` veya yeni bir
  `runtime_incident_<timestamp>/` klasörü)
- [ ] `docs/RUNBOOK.md`, zaman damgası ve artifact bağlantılarıyla
  güncellenmeli

Yalnızca bu beş madde de gerçek kanıtla işaretlendiğinde B036
`READY_FOR_RETEST`'e geçebilir; bu noktadan sonra B031 resmi gate'i
yeniden koşulabilir.
