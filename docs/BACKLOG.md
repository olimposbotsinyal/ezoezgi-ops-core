# BACKLOG — EzoEzgi Ops

> PLAN.md içindeki aktif sprint kapsamı dışında kalan tüm istekler buraya yazılır.
> Sprint ortasında PLAN.md kapsamı değişmez; yeni fikir/istek önce buraya düşer.

| ID | Başlık | Öncelik | Durum | Faz | Sorumlu Ajan | Not |
|---|---|---|---|---|---|---|
| B001 | Kök `README.md` + proje özeti | Yüksek | Açık | Faz 0 | — | PLAN.md T3 ile aynı, referans amaçlı |
| B002 | Config loader — hot reload | Yüksek | Açık | Faz 0 | Orchestrator | PLAN.md T5 |
| B003 | Ollama local runtime kurulum betiği | Yüksek | Açık | Faz 1 | Bridge Agent | Model indirme + sağlık kontrolü |
| B004 | Whisper STT entegrasyonu | Orta | Açık | Faz 3 | Voice Agent | services/stt-whisper iskeleti boş |
| B005 | TTS servis seçimi (local model kararı) | Orta | Açık | Faz 3 | Voice Agent | DECISIONS.md'de ADR gerekiyor |
| B006 | Finance Engine veri modeli tasarımı | Orta | Açık | Faz 2 | Finance Agent | Harcama/gelir şeması |
| B007 | Social Engine onay akışı (auto-post guard) | Yüksek | Açık | Faz 5 | Social Agent | §8 güvenlik mekanizmasına bağımlı |
| B008 | Research Engine — web erişim politikası | Orta | Açık | Faz 2 | Research Agent | Offline/online fallback davranışı |
| B009 | Doc Ingestion — desteklenecek dosya formatları | Düşük | Açık | Faz 2 | Doc Agent | PDF/DOCX/TXT önceliklendirme |
| B010 | GSM Gateway — sağlayıcı/donanım seçimi | Orta | Açık | Faz 3 | Device Agent | SIM/modem tipi netleşmeli |
| B011 | Bluetooth cihaz senaryoları | Düşük | Açık | Faz 3+ | Device Agent | Kapsam MASTER_ROADMAP §4'te ertelendi |
| B012 | Gesture-vision — privacy-by-default doğrulama | Yüksek | Açık | Faz 5 | Device Agent | Görüntü diske yazılmama garantisi |
| B013 | Admin Panel — ilk wireframe | Düşük | Açık | Faz 5 | — | apps/admin-panel şu an boş |
| B014 | Mobile PWA — offline cache stratejisi | Orta | Açık | Faz 3 | — | Service worker tasarımı |
| B015 | Prometheus/Grafana dashboard seti | Düşük | Açık | Faz 6 | — | infra/monitoring iskeleti boş |
| B016 | OSV taramasının CI'ya bağlanması | Orta | Açık | Faz 4 | — | CI henüz kurulu değil |
| B017 | Audit log şeması (append-only) | Yüksek | Açık | Faz 4 | Orchestrator | data/audit formatı netleşmeli |
| B018 | Risk seviyesi taksonomisi detaylandırma | Yüksek | Açık | Faz 4 | Orchestrator | low/medium/high/irreversible kriterleri; T13'te ilk çalışan sürüm (`risk_engine.py` + `tool_risk_policy.yaml`) teslim edildi, Faz 4'te daha ayrıntılı kriterler/hardening bekliyor |
| B019 | Çok dilli genişleme (TR/EN dışı) | Düşük | Açık | Backlog | Bridge Agent | Şimdilik kapsam dışı, ADR gerektirir |
| B020 | Native mobil uygulama ihtiyacı değerlendirmesi | Düşük | Açık | Backlog | — | PWA yeterli mi — ayrı ADR |
| B021 | Exchange connector abstraction (Binance/Bybit/BtcTurk vb.) | Yüksek | Açık | Faz 2 | Finance Agent | Ortak arayüz, borsa-özel adaptörler; gerçek bağlantı bu maddede yok |
| B022 | Order sizing policy | Yüksek | Açık | Faz 2 | Finance Agent | L2/L3 eşiklerini belirleyen pozisyon büyüklüğü kuralları; policies/risk/finance.yaml ile ilişkili |
| B023 | Stop-loss / take-profit policy | Yüksek | Açık | Faz 2 | Finance Agent | Otomatik risk sınırlama kuralları; execution mock (PLAN.md T19) sonrası netleşecek |
| B024 | Circuit breaker (max günlük zarar) | Yüksek | Açık | Faz 2 | Finance Agent | Eşik aşılınca tüm L2/L3 işlemleri otomatik durdurma; RUNBOOK kill switch ile ilişkili |
| B025 | API permission validator (withdraw=false kontrolü) | Yüksek | Açık | Faz 2 | Finance Agent | Bağlantı kurulmadan önce API key izinlerini doğrular; MASTER_ROADMAP.md §5 zorunlu ön koşul |
| B026 | Gelisen_Bot parity checklist | Yüksek | Açık | Faz 2 | Finance Agent | `docs/imports/GELISEN_BOT_PARITY_CHECKLIST.md`; T19'un yeniden açılması için ön koşul (ADR-011) |
| B027 | Telegram command adapter tasarımı | Orta | Açık | Faz 2 | Bridge Agent | Gelisen_Bot'un ikili Telegram kütüphanesi (python-telegram-bot + telebot) karmaşasından kaçınacak tek-kütüphane adapter; `GELISEN_BOT_ANALIZ_RAPORU.md` §3 referans |
| B028 | Strategy abstraction katmanı | Orta | Açık | Faz 2 | Finance Agent | Gelisen_Bot `strategies/base_strategy.py`/`strategy_v1.py` deseninden ilham; EzoEzgi Finance Agent'a uyarlanacak |
| B029 | Dry-run replay test altyapısı | Orta | Açık | Faz 2 | Finance Agent | Geçmiş piyasa verisiyle strateji/risk motorunu gerçek para olmadan (L1) tekrar oynatma; Gelisen_Bot'ta karşılığı yok, yeni yetenek |
| B030 | Kullanıcı borsa credential encryption | Yüksek | Açık | Faz 2 | Finance Agent | Gelisen_Bot'ta plaintext DB saklama tespit edildi (`GELISEN_BOT_SECURITY_NOTES.md` P1); EzoEzgi'de envelope encryption zorunlu, B021 öncesi ön koşul |
| B031 | Gerçek TR-EN çeviri/NLU entegrasyonu | Yüksek | **Kısmen tamamlandı — Ollama runtime çöküyor (llama-server crash), yeni engel** | Faz 1 | Bridge Agent | Adaptör + bayrak + quality gate (golden set + `tools/eval_nlu.py` + eşikler) tamam. **2026-08-13T00:01:19Z:** ilk canlı ölçüm — 5/5'ten 4 FAIL, kök neden: istemci timeout'u (2.0s) gerçek inference'tan (~6s) kısaydı. **Düzeltildi** (`model_client.py::DEFAULT_TIMEOUT_SECONDS`→30.0s + `OLLAMA_TIMEOUT_SECONDS` env override, 6 yeni test, 142/142 regresyon yeşil). **2026-08-13T00:13:05Z: ikinci canlı ölçüm** (`reports/nlu_eval_20260813.md`) — timeout düzeltmesi doğrulandı çalışıyor (istekler artık kesilmiyor, gerçek sunucuya ulaşıyor, p95 12.89s) ama **yeni bir engelle karşılaşıldı**: Ollama'nın `llama-server` alt süreci her istekte çöküyor (`HTTP 500`, `"llama-server process has terminated: exit status 0xc0000005"` — Windows access violation; `curl` ile manuel doğrulandı). RAM yeterli (66.9GB/23GB boş) — kaynak kısıtı değil, muhtemelen Ollama/llama.cpp build'i ile bu makine arasında bir uyumsuzluk/bug. Sonuçlar sayısal olarak ilk koşuyla aynı (%30/%0/%100) ama nedeni farklı. B031 hâlâ **Partial / FAILED_THRESHOLDS**. Sonraki adım netleştirilmeyi bekliyor: farklı/daha küçük bir model denemek (ör. `phi3`, `llama3.2:1b`), Ollama'yı güncellemek/yeniden kurmak, veya crash'i ayrıca teşhis etmek |
| B032 | Python bağımlılık dosyası (dev dependencies) | Orta | Açık | Faz 0/1 | — | `pytest` + `pyyaml` şu an yalnızca `.venv`'e ad-hoc kuruldu; reproducibility için `pyproject.toml` `[project.dependencies]` veya `requirements-dev.txt` eklenmeli |
| B033 | Alias matcher'da Türkçe ek toleransı | Düşük | Açık | Backlog | Bridge Agent | `alias_matcher.py` yalnızca tam kelime eşleşmesi yapıyor; "ezgi'ye" gibi ek almış formlar kapsam dışı bırakıldı (bkz. `tests/test_alias_matcher.py` "EZGİYE söyle" negatif vakası) |
| B034 | `cli-runner` genel whitelist mekanizması (T12) | Yüksek | **Kapalı** | Faz 0 (Gün 10-11) | — | 2026-08-13'te T12 ile tamamlandı: `tools/cli-runner/src/runner.py` + `config/cli_whitelist.json` (bkz. ADR-012); `echo_runner.py` artık bunu kullanıyor |
| B035 | Risk taksonomisi uyumlulaştırma (genel vs finans) | Orta | Açık | Faz 2 | Orchestrator | T13'ün genel `low/medium/high/irreversible` şeması (`tool_risk_policy.yaml`) ile finansın `L0-L3` şeması (T18, MASTER_ROADMAP.md §5) şu an paralel/ayrı; Faz 2'de finans task'ları genel registry'ye eklenirken iki şemanın nasıl ilişkileneceği netleşmeli |
| B036 | Ollama Windows `0xc0000005` (`llama-server` crash) araştırılması | **Yüksek** | Açık — **kök neden log-kanıtlı izole edildi (GPU/Vulkan), kalıcı çözüm bekliyor** | Faz 1 | Bridge Agent | 2026-08-13T00:38:34Z stabilizasyon kapısı (`reports/runtime_diag_20260813T003834Z/`): 4/4 tekli test aynı `0xc0000005` ile çöktü, `OLLAMA_NUM_GPU=0` yetersiz kaldı (Vulkan cihazı log'da hâlâ görünüyordu). **2026-08-13T01:00Z derin triage** (`reports/runtime_incident_20260813T004855Z/`): Host fingerprint — NVIDIA sürücü **442.94/CUDA 10.2** (Ollama'nın `cuda_v12/v13` beklentisinden çok eski) + Vulkan Instance **1.2.131** (eski) + Event Viewer'da 20 özdeş çöküş kaydı (`Hatalı modül: unknown`). **GPU/Vulkan izolasyon matrisi (A-E):** `OLLAMA_VULKAN=false` VE `OLLAMA_LLM_LIBRARY=cpu` — ikisi de sunucu loglarıyla **kanıtlanmış** şekilde Vulkan cihazını hiç başlatmıyor (`library=cpu` seçiliyor, `Vulkan`/`Quadro RTX 3000` log'da hiç geçmiyor) ve bu modda **5/5 çağrı başarılı, 0 çöküş**. Vulkan cihazı başlatıldığında (baseline) ise **5/5 çöküş**. **Sürüm A/B:** mevcut `0.32.9` çöküyor; önceki `0.30.0` (gerçekten kurulup test edildi, sonra `0.32.9`'a geri yüklendi) varsayılan ayarlarla çökmüyor — ama log'a göre bu sürüm de Vulkan cihazını bu donanımda hiç seçmiyor (otomatik CPU'ya düşüyor), yani "0.30.0'da düzeltilmiş" değil, farklı GPU-keşif davranışı. **Sonuç: GPU/Vulkan sürücü etkileşimi kök neden olarak kesin izole edildi (log kanıtlı).** Reprodüksiyon scripti: `scripts/repro_ollama_crash.ps1`. Upstream issue taslağı hazır: `reports/runtime_incident_20260813T004855Z/OLLAMA_GITHUB_ISSUE_DRAFT.md` (durum: **AÇILDI**, bkz. aşağıdaki güncelleme). **Kabul kriteri (değişmedi):** B031 quality gate'i yeniden koşmadan önce **varsayılan (Vulkan etkin) ortamda** 50/50 başarılı, 0 HTTP 500, 0 `0xc0000005`. CPU-only workaround (`OLLAMA_VULKAN=false`) çökmüyor ama gecikmesi (7.7s-26.9s/çağrı) B031'in `latency_p95≤2.5s` eşiğini muhtemelen geçemez. **Sonraki adımlar:** (1) NVIDIA sürücüsünü güncellemek (442.94 → güncel) ve Vulkan modunda yeniden test etmek, (2) upstream issue'yu göndermek, (3) sürücü güncellemesi çözmezse B031'in gecikme eşiğini CPU-only gerçeğine göre ayrı bir ADR ile yeniden değerlendirmek. **2026-08-13T01:19Z post-triage aksiyonlar:** Upstream issue bu ortamdan açılamamıştı (gh CLI/token yok) — gönderime hazır tam paket `reports/runtime_incident_20260813T004855Z/ISSUE_READY_PACKAGE.md` hazırlandı. **2026-08-13 ~05:02 (+03:00) güncelleme: Upstream issue durumu READY_TO_SUBMIT → AÇILDI.** Kullanıcı paketi kullanarak manuel gönderdi: <https://github.com/ollama/ollama/issues/17716> (bkz. `docs/RUNBOOK.md` "B036 Upstream Issue — Durum"). B036 genel durumu **IN_PROGRESS** olarak kalıyor (issue açılması B036'yı çözmüyor, yalnızca upstream takibini başlatıyor). Geçici CPU-only workaround formalize edildi: `docs/ops/OLLAMA_WORKAROUND_CPU_ONLY.md` (`OLLAMA_VULKAN=false`/`OLLAMA_LLM_LIBRARY=cpu`) — yalnızca bilgilendirici koşular için, resmi B031 kararını etkilemez. **B036 çıkış kriterleri (resmi, değişmez):** (1) varsayılan (Vulkan etkin, workaround YOK) profilde 50/50 başarılı `/api/generate` çağrısı, (2) 0 `HTTP 500`, (3) loglarda 0 `0xc0000005`, (4) yalnızca bunlar sağlandıktan sonra B031 resmi gate'i yeniden koşulur. **Sürücü güncelleme takip maddesi (açık, sonuç bekliyor):** hedef — NVIDIA Quadro RTX 3000 sürücüsünü `442.94`'ten güncel bir sürüme yükseltmek (hedef sürüm/pin henüz seçilmedi, sonraki turda netleştirilecek); sonuç bu satıra ve `docs/RUNBOOK.md`'ye loglanacak. **2026-08-13T02:33Z Post-upstream Experiment Batch #1** (`reports/runtime_incident_20260813T004855Z/exp_01_post_upstream/`, n=100, 5 senaryo × 20 çağrı, script: `scripts/repro_b036_batch.ps1`): A (Vulkan ON baseline) 20/20 çöküş; B (CPU-only) 20/20 başarı; C (`OLLAMA_NUM_PARALLEL=1`) 20/20 çöküş; D (`num_ctx=256`) 20/20 çöküş; E (her çağrıda temiz restart) 20/20 çöküş. **Toplam 80/100 çöküş — tüm Vulkan-etkin varyasyonlarda %0 başarı, istisnasız; yalnızca CPU-only crash-free.** Önceki n=5 bulgusu n=20/senaryo ile tam doğrulandı. **B036 kararı: IN_PROGRESS (READY_FOR_RETEST'e geçilmedi — hiçbir Vulkan-etkin senaryo crash-free değil).** B031 gate atlandı, neden: `blocked_by_runtime_instability`. Upstream takip yorumu taslağı hazırlandı: `reports/runtime_incident_20260813T004855Z/exp_01_post_upstream/upstream_comment_draft_17716.md` (henüz gönderilmedi, manuel gönderim bekliyor — bkz. `docs/RUNBOOK.md` "Upstream Follow-up Prepared"). **2026-08-13 ~05:47 (+03:00): Watch mode active.** Takip yorumu kullanıcı tarafından issue #17716'ya manuel eklendi (permalink: <https://github.com/ollama/ollama/issues/17716#issuecomment-5275320830>). Maintainer yanıtı gelene kadar geniş kapsamlı yeni deney partisi çalıştırılmayacak — protokol: `docs/ops/B036_UPSTREAM_WATCH_PROTOCOL.md`, kontrol günlüğü: `reports/runtime_incident_20260813T004855Z/upstream_watch_log.md`, tetikleyici bazlı doğrulama planı: `reports/runtime_incident_20260813T004855Z/validation_on_trigger.md`. **B036 durumu değişmedi: IN_PROGRESS.** Watch checks are automated via `scripts/watch_b036_upstream.py`. **Resilience layer added:** `services/model-gateway/src/model_gateway/` — sıralı, açık-loglu çok-sağlayıcılı fallback (ollama→local_alt→remote, varsayılan yalnızca ollama, remote politika kapılı). B036'yı çözmez, yalnızca tek sağlayıcı çökmesinin sistemi tamamen durdurmasını önler; bkz. `docs/ops/MODEL_FALLBACK_RUNBOOK.md`. **CPU-only runtime verification guard eklendi** (`runtime_verify.py`): `OLLAMA_VULKAN=false` zorlamasının yalnızca best-effort (harici sürece etkisiz) olması riskini kapatmak için, Ollama birincil olarak seçilmeden önce kanıt-temelli bir doğrulama kapısı çalışır — operatör marker dosyası (pozitif kanıt), Windows Event Viewer'da bilinen çöküş imzası taraması (negatif kanıt), `/api/ps` VRAM kontrolü (zayıf negatif kanıt). **ÖNEMLİ:** `OLLAMA_CPU_VERIFY_STRICT=true` varsayılanıyla, operatör marker dosyası oluşturmadıkça doğrulama genellikle `UNKNOWN` döner ve Ollama STRICT modda **birincil olarak seçilmez** — bu, kasıtlı ve `docs/ops/MODEL_FALLBACK_RUNBOOK.md`'de yüksek sesle belgelenmiş bir tasarım kararıdır, "sessiz davranış değişikliği yok" ilkesi gereği burada de not düşülüyor. Yeni reason code'lar: `PRIMARY_RESTRICTED_CPU_UNVERIFIED`, `FALLBACK_EXHAUSTED`. Yeni audit event'leri: `OLLAMA_CPU_PREFLIGHT_CHECKED`, `OLLAMA_PRIMARY_RESTRICTED` (tümü ortak bir `trace_id` ile korelasyonlu). **SLI/SLO/alert/ops otomasyonu eklendi:** `services/model-gateway/src/model_gateway/metrics.py` (bellek-içi sayaç/histogram/gauge, canlı Prometheus endpoint'i yok — bkz. `docs/BACKLOG.md` B015), `docs/ops/SLO_MODEL_GATEWAY.md` (Availability/Degradation/Primary-Usage/Preflight-Confidence SLI'ları, burn-rate örnekleri), `docs/ops/ALERT_PLAYBOOK_MODEL_GATEWAY.md` (5 alert tipi + ilk aksiyonlar, `infra/monitoring/prometheus/model_gateway_alerts.yaml` örnek kural seti — henüz hiçbir canlı Prometheus/Alertmanager tarafından değerlendirilmiyor), `scripts/ops/daily_gateway_smoke.ps1` (günlük sağlık kontrolü — config sanity, preflight/sağlık anlık görüntüsü, yıkıcı-olmayan sentetik classify probu, audit-append doğrulaması, exit code 0/1/2, kanıt paketi `reports/daily_smoke_<UTC>/`), `scripts/ops/package_gateway_incident.ps1` (olay kanıt paketleyici — audit dilimi + maskelenmiş config parmak izi + git SHA/değişen dosyalar, `reports/incidents/gateway_<timestamp>/`). Remote policy gate zayıflatılmadı (`remote_policy_gate=required` sabit kaldı, daily smoke bunu ayrıca kritik bulgu olarak kontrol ediyor). **Canlı metrics + izleme yığını (Commit G/H):** `scripts/ops/serve_metrics.py` — gerçek, test edilmiş `GET /metrics` (stdlib, bağımlılıksız). Keşif ile doğrulandı: bu makinede Docker/Prometheus/Alertmanager/FastAPI **kurulu değil**. `infra/monitoring/prometheus/prometheus.yml` + `infra/monitoring/alertmanager/alertmanager.yml` (Aşama 1/observe-only, `null-receiver`) geçerli ama okunmuyor. `scripts/ops/emit_synthetic_gateway_signals.py` + `scripts/ops/verify_alert_pipeline.ps1` gerçekten çalıştırıldı — bu makinede dürüstçe **exit code 1 (kısmi)** üretti (metrics UP, Prometheus/Alertmanager yok). Geliştirme sırasında bulunan ve düzeltilen gerçek bir hata: sentetik sinyal başlangıçta ayrı bir süreçte enjekte ediliyordu ve `/metrics` çıktısında hiç görünmüyordu (`get_metrics()` süreç-içi singleton) — `serve_metrics.py`'ye `--enable-debug-injection` + `POST /debug/inject_synthetic` eklenerek düzeltildi, elle doğrulandı. Gate A-D (24s gözlem, %99 scrape, sentetik alert doğrulama, regresyon yok) tam sağlanamadığından **warning/critical routing açılmadı**, rollout Aşama 1'de kalıyor. Tam kılavuz: `docs/ops/MONITORING_STACK_RUNBOOK.md`. **2026-08-13 Cross-process metrik sink'i eklendi (Commit I/J):** `/metrics`'in "yalnızca kendi sürecinde biriken metrikleri gösterir" sınırlaması `METRICS_SINK=jsonl_append` (yeni varsayılan) ile kapatıldı — `model_gateway/metrics_sink.py` (`JsonlAppendSink`: kilit + boyut/gün rotasyonu + retention + sınırlı-kardinalite koruması) ve `model_gateway/metrics_aggregate.py` (JSONL'den okuyup tek bir Prometheus görüntüsüne birleştirme, sert okuma hatasında `AggregationError`) eklendi; `serve_metrics.py`, `/metrics` her çağrıldığında bu aggregator'ı kullanacak şekilde güncellendi ve aggregator sert hatasında **503 + tanılama nedeni** döner, ayrıca öz-metrikler (`metrics_sink_write_failures_total`, `metrics_events_dropped_total`, `metrics_aggregator_read_failures_total`) yanıta eklendi. **Gerçek 2 ayrı OS süreci + `serve_metrics.py` üçüncü süreç ile elle doğrulandı** (curl ile gerçek HTTP GET, iki yazıcının toplam katkısı tek counter'da doğru toplandı). Geriye dönük uyumlu: `METRICS_SINK=in_memory` eski davranışı opt-in korur, `classify()` sözleşmesi/fallback mantığı/remote varsayılanları değişmedi. Bilinen ödünleşimler (eventual-consistency penceresi, scrape gecikmesi, öz-metriklerin süreç-yerelliği): `docs/ops/MONITORING_STACK_RUNBOOK.md` "Bilinen ödünleşimler". Tam regresyon 205/205 yeşil. **2026-08-13 Performans donusumu (Commit K/L):** `/metrics`'in her scrape'te tüm JSONL geçmişini yeniden taraması giderildi — `model_gateway/metrics_index.py` (dosya kimligi `(st_dev, st_ino)` uzerinden, rename/rotasyona SEFFAF, kalici state `data/metrics/*.state.json`), `IncrementalAggregator` (`metrics_aggregate.py` — yalnizca yeni byte'lari okur, `compact_to_registry()`'yi DOGRUDAN yeniden kullanarak tam-yeniden-taramayla BIREBIR AYNI cikti uretir, testle kanitlandi) ve `CachedMetricsRenderer` (`metrics_cache.py` — `METRICS_AGG_CACHE_TTL_SEC` icindeki tekrar istekleri onbellekten yanitlar) eklendi. Yeni guardrail'ler: `METRICS_AGG_MAX_SERIES`/`METRICS_AGG_MAX_EVENTS_PER_SCRAPE` (asim durumunda veri kaybi degil, guvenli dusurme/erteleme + oz-metrik: `metrics_aggregator_series_dropped_total`, `metrics_aggregator_events_deferred_total`, ayrica onbellek icin `metrics_aggregator_cache_hits_total`/`_misses_total`/`_rebuild_duration_ms`). Gercek benchmark (`scripts/ops/benchmark_metrics_scrape.py`, 5000 olay/~6 rotasyon dosyasi): p50 scrape gecikmesi tam-yeniden-taramada ~249ms, artimli+onbeleksiz ~45ms (**~5.6x**), artimli+onbellek isabetinde ~0.004ms — ayrintili tablo ve trafik-bazli ayar rehberi: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Performans ayar rehberi". Bozuk/eksik state dosyasi SERT hata degildir, otomatik yeniden insa edilir (auto-heal, testle kanitlandi). Geriye donuk uyumlu: `classify()` sozlesmesi/fallback/remote politika degismedi, `AggregationError`→503 davranisi korundu. B036 durumu degismedi: **IN_PROGRESS** (bu iş gözlemlenebilirlik altyapısını genişletir, B036'nın kendisini çözmez). **2026-08-13 Go-live paketi eklendi (Commit M/N):** `scripts/ops/run_observability_gates.ps1` + `observability_gates_core.py` (otomatik Gate A-E: metrics availability, scrape success, 4 sentetik alert modunun görünürlüğü, Alertmanager alma yolu, classify regresyon smoke — `gate_report.md`+`gate_results.json`, exit code 0/1/2, `overall_exit_code` haritalaması: FAIL varsa 2, PARTIAL/SKIPPED varsa 1, hepsi PASS ise 0). Bu makinede **gerçek elle çalıştırıldı, sonuç FAIL (exit 2)**: A/B/C PASS, D SKIPPED (Alertmanager kurulu değil), E FAIL — kök neden B037 (aşağıya bakın), classify/fallback sözleşmesiyle İLGİSİZ. `scripts/ops/calibrate_alert_thresholds.py` (son N saatlik gerçek/sentetik-hariç veriden WARN/CRIT önerisi + gerekçe + yanlış-pozitif riski notu) — bu makinede gerçek trafik olmadığından `INSUFFICIENT_DATA`, mevcut varsayılanlar korundu (dürüst, fabrike edilmemiş sonuç). `scripts/ops/build_observability_signoff.py` (git SHA + gerçek tam pytest çalıştırması + en son gate sonucu + GO/CONDITIONAL-GO/NO-GO kararı, `SIGNOFF.md`/`SIGNOFF.json`) — gate_results.json yoksa NO-GO döner, ASLA varsayılan GO uydurmaz. `scripts/ops/rollback_observability.ps1` + `rollback_observability_core.py` (varsayılan **dry-run**, `-Apply` ile gerçek uygulama; yalnızca `alertmanager.yml` route receiver'larını metin-tabanlı, hedefli değişiklikle Aşama 1'e döndürür — YAML'i tam yeniden serilestirmez ki aciklama satirlari KORUNSUN; `/metrics`'i ASLA kapatmaz, `data/audit/`+`data/metrics/` verisine ASLA dokunmaz; idempotent). Hem dry-run hem apply, hem "zaten güvenli" hem "gerçek escalate edilmiş durumu düzeltme" (geçici olarak `warning-sink`/`critical-sink`'e alınıp geri alındı, git diff ile doğrulandı, sonra orijinal dosyaya geri döndürüldü) senaryolarında elle test edildi. Kanonik "Go/No-Go checklist": `docs/ops/MONITORING_STACK_RUNBOOK.md`. Tam regresyon 265/265 yeşil (Bash üzerinden). **2026-08-13 NO-GO engelleri kapatıldı (Commit O/P):** Önceki go-live sign-off NO-GO idi (Gate E FAIL — B037, Gate D SKIPPED — Alertmanager kurulu değildi). B037 düzeltildi (bkz. B037 satırı). Gate D için GERÇEK Prometheus v3.13.2 + Alertmanager v0.33.1 indirilip yerel çalıştırıldı (repo'ya commit edilmedi), sentetik `circuit-open-stuck` sinyali gerçekten `firing`'e geçti ve Alertmanager bunu ALDI (`null-receiver`'a yönlendirilerek, observe-only KORUNDU) — kanıt: `reports/gate_d_real_validation_20260813T072946Z/` (`git add -f`). `run_observability_gates.ps1` yeniden çalıştırıldı: **5/5 gate PASS** (`reports/go_live_gates_20260813T073109Z/`). `build_observability_signoff.py` yeniden çalıştırıldı: karar **GO** (`reports/observability_signoff_20260813T073140Z/`, ikisi de `git add -f`). Tam suite 278/278 yeşil hem Bash hem native PowerShell'de. **2026-08-13 Post-GO sertleştirme (Commit Q/R):** `scripts/ops/calibrate_alert_thresholds.py` **v1**'e genişletildi — 24h/7d/14d pencereleri, güven skoru (LOW/MEDIUM/HIGH — birden fazla pencerede yeterli VE tutarlı veri gerektirir), örnek boyutu yeterliliği, değişiklik-etkisi notu; `calibration_v1.md`/`.json` + kesinlikle AYRI bir `proposed_threshold_patch.yaml` (politika koruması yapısaldır — script'te config'i değiştiren HİÇBİR kod yolu yok, yalnızca `NEVER_AUTO_APPLY`/`PROPOSAL_ONLY_NOT_APPLIED` etiketli bir öneri dosyası üretir). `scripts/ops/detect_observability_drift.py` + `observability_drift_core.py` eklendi — `infra/monitoring/baseline/metrics_manifest_v1.json` (gerçek router.py/runtime_verify.py/compat.py kod taramasından elle çıkarılmış metrik/etiket envanteri) + `alerts_checksum_v1.txt` (SHA256) baseline'larına karşı metrik şeması/alert kuralları/`remote_enabled`/`ollama_cpu_verify_strict` drift'i tespit eder; exit code 0/1/2, `remote_enabled`→`True` her zaman CRITICAL. Gerçek tahrifat (alert dosyası değiştirildi, bilinmeyen metrik+etiket enjekte edildi) ile test edildi, her ikisi de doğru tespit edildi. `infra/monitoring/profiles/persistent/` eklendi (üretim-güvenli Prometheus/Alertmanager profili, `promtool check config`/`amtool check-config` ile GERÇEKTEN doğrulandı — `rule_files` yolunun config dosyasının kendi dizinine göre çözüldüğü gerçek bir hata bu doğrulamada yakalanıp düzeltildi; Alertmanager'ın `${VAR}` sözdizimini kendisinin ÇÖZMEDİĞİ ampirik olarak doğrulanıp README'ye not düşüldü). `scripts/ops/weekly_observability_review.ps1` + `weekly_review_core.py` eklendi — haftalık ritüel (hafif gate snapshot + drift + 7 günlük oranlar → GREEN/YELLOW/RED, `reports/weekly_observability_<YYYY-WW>/review.md`'ye EKLEME, üzerine yazma değil) — gerçek çalıştırıldı, iki ardışık çalıştırmanın ikisinin de dosyada korunduğu doğrulandı. Tam regresyon 335/335 yeşil hem Bash hem native PowerShell'de. **2026-08-13 İnsan onaylı eşik değişikliği iş akışı (Commit S/T):** `infra/monitoring/governance/threshold_proposal_schema_v1.json` (proposal alan sözleşmesi, `checksum` bütünlük alanı) + `scripts/ops/threshold_governance_core.py` (şema doğrulama, SHA256 checksum, `check_apply_eligibility` — şema+decision=APPROVE+proposal_id eşleşmesi+checksum eşleşmesi TÜMÜ sağlanmalı) + `scripts/ops/generate_threshold_proposals.py` (`calibrate_alert_thresholds.py`'yi sarmalar, yalnızca CALIBRATED+mevcuttan-farklı öneriler için proposal üretir) + `scripts/ops/create_threshold_review_record.py` (APPROVE/REJECT/NEEDS_DATA + gerekçe kaydı) eklendi. Ardından `scripts/ops/threshold_apply_core.py` (hedefli regex ile `model_gateway_alerts.yaml`'i yamalar — tam YAML yeniden-serileştirme YAPMAZ, `promtool check rules` ile gerçekten doğrulandı; yedekleme/geri-yükleme; `approved_checksums_ledger.jsonl` append-only defter yardımcıları) + `scripts/ops/apply_threshold_proposal.ps1` (VARSAYILAN dry-run, `-Apply` ile gerçek uygulama — yedek+ledger+gerçek `AuditLogger` denetim kaydı) + `scripts/ops/rollback_threshold_apply.ps1` (VARSAYILAN dry-run, `-Apply` ile checksum-birebir geri yükleme) eklendi. `observability_drift_core.py::check_alert_rules_checksum_drift`'e geriye-uyumlu `approved_checksums` parametresi eklendi — ledger'da kayıtlı bir checksum artık CRITICAL SAYILMIYOR (severity=NONE, "onaylı değişiklik" gerekçesiyle). **Gerçek uçtan uca doğrulandı:** sentetik 300 istek/15 null-intent'ten gerçek proposal+review üretildi; dry-run hiçbir dosyayı DEĞİŞTİRMEDİ (checksum/`git diff` ile doğrulandı); `-Apply` gerçek dosyayı yamaladı (`promtool check rules` ile geçerlilik doğrulandı), gerçek yedek/ledger/audit kaydı oluşturdu; rollback dosyayı UYGULAMA-ÖNCESİ checksum'a BİREBİR geri getirdi; REJECT kararı VE tahrif edilmiş (review-sonrası değiştirilmiş) proposal, ikisi de exit code 2 ile ENGELLENDİ, hiçbir dosya mutasyona uğramadı. Commit S/T testleri: 63 yeni test (threshold_governance_core, generate/create CLI'ları, threshold_apply_core, drift istisnası) — tam regresyon yeşil. Ayrıntı + RACI + acil durum yolu: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Eşik Değişikliği SOP". **2026-08-13 Governance v1.1 sertleştirme (Commit U/V):** `--decision APPROVE_EMERGENCY` eklendi — `incident_id`/`justification`/`timebox_hours` (≤24s)/`retro_review_due_utc` hem review_record oluşturulurken (CLI) hem apply anında (`check_apply_eligibility`, review_record.json elle değiştirilse bile) MAKİNE TARAFINDAN doğrulanır; normal APPROVE/REJECT/NEEDS_DATA kayıtları BİREBİR değişmedi (geriye dönük uyumlu). `threshold_apply_core.py`'deki regex yama kaydı artık her hedef için TAM 1 eşleşme zorunlu kılar (0/>1 eşleşme fail-loud engellenir, `multi_target=True` açıkça işaretlenmedikçe); üç alert (`PRIMARY_RESTRICTED_PERSISTENT`, `PREFLIGHT_UNKNOWN_PERSISTENT`, `CIRCUIT_OPEN_STUCK`) `calibrate_alert_thresholds.ALERT_ENV_VAR_MAP`'te env var eşlemesi olmadığı için "non-patchable" kaydına eklendi ve her apply raporunda nedenleriyle listelenir. `apply_threshold_proposal.ps1 -VerifyReload` (opsiyonel) eklendi — gerçek apply SONRASI `promtool check rules`/`amtool check-config`/Prometheus-Alertmanager erişilebilirlik kontrolleri çalıştırır, hiçbirini fabrike etmez (yapılandırılmamış → `VERIFICATION_SKIPPED`), genel durum FAIL ise exit code 3 (0/2'den ayrı). **Gerçek makinede çalıştırıldı:** `promtool check rules` GERÇEKTEN PASS verdi, `amtool check-config` `infra/monitoring/profiles/persistent/alertmanager.yml` üzerinde GERÇEK bir sorun (`invalid URL: unsupported scheme ""`, bilinen `${VAR}` yer tutucusu sınırlaması, Commit R'de zaten belgeli) nedeniyle GERÇEKTEN FAIL verdi — fabrike edilmedi. `scripts/ops/check_emergency_review_overdue.py` (yeni CLI) + `observability_drift_core.check_emergency_review_overdue_drift` (drift pipeline'ına otomatik entegre) — ledger'daki her acil durum girdisi için vade geçmiş+takipsizse CRITICAL `emergency_governance` bulgusu üretir; takip eden normal onay (aynı alert_name, sonraki timestamp, is_emergency=false) varsa bulgu üretilmez. `rollback_threshold_apply.ps1` raporu artık kaynak apply/review dosya yollarını VE rollback-sonrası drift anlık görüntüsünü (`check_alert_rules_checksum_drift` yeniden kullanılarak) içerir. Gerçek uçtan uca doğrulandı (emergency dry-run/apply/rollback zinciri, VerifyReload PASS+FAIL karışık senaryo, overdue/followup/cross-alert/emergency-zinciri drift matrisi) ve ardından dosya pristine duruma geri döndürüldü. Commit U/V testleri: ~57 yeni test (emergency validation matrisi, emergency CLI, patch expansion/exactly-one-match/idempotency, VerifyReload state aggregation, overdue drift matrisi, rollback linkage) — tam regresyon (Bash+PowerShell) yeşil. B036 durumu değişmedi: **IN_PROGRESS** (bu iş gözlemlenebilirlik altyapısını genişletir/olgunlaştırır, B036'nın kendisini çözmez). **2026-08-13 Governance v1.2 PILOT denemeleri (Commit W/X, hepsi VARSAYILAN KAPALI):** `scripts/ops/emergency_chain_core.py` — checksum-zinciri sürekliliği ile RESOLVED/BROKEN_CHAIN/NO_FOLLOWUP sınıflandırması (`GOV_EMERGENCY_CHAIN_MATCHING=1`), v1.1'in alert_name-only kontrolünün yanlış-pozitif "çözüldü" riskini kapatır; `run_emergency_chain_trial.py` (salt-okunur v1.1/v1.2 karşılaştırma). `scripts/ops/emergency_legitimacy_core.py`+`check_emergency_legitimacy.py` — bilet-format regex + none/mock/jira_stub sağlayıcı stub'ı (PILOT'ta gerçek ağ/sır YOK), `create_threshold_review_record.py`'nin opsiyonel `--legitimacy-report-path`'i yalnızca bilgi amaçlı, apply uygunluğunu ETKİLEMEZ. `apply_threshold_proposal.ps1 -AutoRollbackOnVerifyFail -AutoRollbackMode safe/strict` (varsayılan KAPALI/safe) — VerifyReload FAIL'de dosyayı otomatik geri yükler (yalnızca FAIL'de, asla SKIPPED/PASS'te), safe modu concurrent-değişiklik durumunu tespit edip iptal eder, audit'e `task=auto_rollback_triggered` kaydı ekler. **Gerçek uçtan uca doğrulandı:** chain trial gerçek bir yanlış-pozitif senaryo yakaladı (v1.1 "çözüldü" derken v1.2 BROKEN_CHAIN/WARN verdi); auto-rollback gerçek `amtool` FAIL'inde (bilinen `${VAR}` sınırlaması, Commit R/V'den) strict modda dosyayı gerçekten geri yükledi, flag kapalıyken dosyayı bilerek mutasyona uğramış bıraktığı da doğrulandı — sonra pristine duruma manuel temizlendi. Commit W/X testleri: ~70 yeni test (chain closure matrisi, legitimacy PASS/FAIL/SKIPPED, auto-rollback tetikleme matrisi, geriye dönük uyumluluk) — tam regresyon (Bash+PowerShell) yeşil. Üç pilot senaryosu çalıştırılıp `reports/v1_2_pilot_<UTC>/pilot_summary.md`'ye kanıtlandı (öneri: bkz. o rapor). Ayrıntı: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Pilot Flags Matrix (default OFF)". **2026-08-13 v1.2 pilot terfi (promotion) kapısı (Commit Y/Z):** `infra/monitoring/governance/pilot_promotion_criteria_v1.json` (her özellik için ölçülebilir kriterler — observation_min_days/min_runs/max_false_positive_rate/max_unresolved_critical/required_evidence_paths/blocker_conditions) + `scripts/ops/evaluate_pilot_promotion.py` (gerçek kanıt dosyalarını tarar, PROMOTE/EXTEND_PILOT/REJECT kararı üretir; blocker koşulları sayısal kriterlerden BAĞIMSIZ REJECT'e zorlar; eksik false_positive_rate verisi PROMOTE'u engeller, asla varsayılan sıfır fabrike edilmez). **Gerçek çalıştırma (mevcut kanıtla):** emergency_chain_matching ve auto_rollback_on_verify_fail EXTEND_PILOT (yetersiz çalıştırma/gözlem penceresi/FP verisi); emergency_legitimacy_required GERÇEK bir REJECT — `provider_is_stub_only` blocker'ı, bugüne kadarki TÜM legitimacy kanıtı `mock`/`jira_stub` sağlayıcı kullandığı için (gerçek bir Jira entegrasyonu hiç kullanılmadı) tetiklendi. Kalıcı terfi durumu `infra/monitoring/governance/pilot_flags_state.json`'da (normal git-tracked, tümü varsayılan `promoted: false`) tutulur — `scripts/ops/promote_pilot_flags.ps1` (VARSAYILAN dry-run, `-Apply` yalnızca PROMOTE kararlı özellikleri uygular, audit'e `task=pilot_flag_promoted`/`pilot_flag_not_promoted` ekler) + `scripts/ops/rollback_pilot_flags.ps1` (VARSAYILAN dry-run, `-Apply` `flag_apply_report.json`'daki `previous_state`'i BİREBİR geri yükler). `threshold_governance_core.py::check_apply_eligibility`'ye ÇİFT-KAPILI (double-gate) `legitimacy_enforcement_active`/`legitimacy_report` parametreleri eklendi (geriye uyumlu varsayılanlar — eski çağrılar değişmeden çalışır) — `GOV_EMERGENCY_LEGITIMACY_REQUIRED=1` TEK BAŞINA hiçbir şeyi engellemez, `pilot_flags_state.json`'da GERÇEK bir PROMOTE kaydı da gerekir. **Gerçek uçtan uca doğrulandı:** izole ama gerçek-kod-yollu kanıtla iki özellik gerçekten promote edildi (state dosyası + audit kaydı doğrulandı), legitimacy blocking'in dört durumu (env=0 geçer, env=1+rapor-yok engeller, env=1+rapor-FAIL engeller, env=1+rapor-PASS geçer) TEK TEK gerçek `apply_threshold_proposal.ps1` çalıştırmalarıyla doğrulandı, ardından her iki terfi de `rollback_pilot_flags.ps1` ile BİREBİR geri alınıp `pilot_flags_state.json`'ın pristine durumuna döndüğü `git diff` ile teyit edildi. Commit Y/Z testleri: ~60 yeni test (kriter doğrulama, karar matrisi, state-geçişi, çift-kapı mantığı, legitimacy enforcement matrisi) — tam regresyon (Bash+PowerShell) yeşil. Ayrıntı: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Pilot Promotion Policy". **2026-08-13 Kanıt olgunlaştırma sprinti (Commit AA/AB):** Commit Y/Z'nin terfi kapısı otomatikti ama kanıt üç yerde eksikti (bkz. `docs/ops/MONITORING_STACK_RUNBOOK.md` "Evidence maturity model"). `scripts/ops/compute_pilot_false_positive_rate.py` + `pilot_fpr_core.py` — üç pilot özelliği için gerçek kanıt dosyalarından "sinyal" toplar (chain_eval BROKEN_CHAIN, apply_report auto_rollback.triggered, legitimacy_report FAIL), `infra/monitoring/governance/pilot_fpr_adjudications.json` (insan tarafından elle doldurulan, `schema_version:1` ile boş başlayan bir defter) ile `signal_id` üzerinden eşleştirir; oran YALNIZCA ≥3 onaylı sinyal varsa hesaplanır (`MIN_ADJUDICATED_FOR_RATE`), aksi halde `status=INSUFFICIENT_DATA`+`false_positive_rate=None` — asla sıfır fabrike edilmez; güven bandı LOW(<10)/MEDIUM(10-29)/HIGH(≥30) — `reports/pilot_metrics_<UTC>/fpr_summary.json`+`.md`. `scripts/ops/export_weekly_observability_json.py` — `weekly_review_core.py::render_review_entry_md()`'nin KENDİ ürettiği (ve yalnızca bu projenin denetlediği) markdown formatını ayrıştırıp `reports/weekly_observability_<YYYY-WW>/review.json`'a dönüştürür (durum/nedenler/fallback+null-intent oranları/üst alertler/incident sayısı). `evaluate_pilot_promotion.py` her iki kaynağı da otomatik tüketir — `fpr_summary.json` VARSA ve ilgili özellik `COMPUTED` ise eski per-evidence-file ortalama davranışının YERİNE geçer (yoksa eski davranış geriye uyumlu korunur); `review.json` VARSA tercih edilir, markdown'a asla geri dönülmez. **Blocker otomasyonu** (plan Commit AB altında öngörülmüştü, ama değerlendirici dosyasındaki değişiklikler FPR/haftalık-kanıt kablolamasıyla iç içe geliştiği için `git add -p` kullanılamadan fiilen Commit AA'da gönderildi — dürüstlük notu, bkz. final rapor): `secrets_committed` artık `scripts/ops/secret_scan_core.py` (desen-tabanlı denylist + allowlist-marker/path, `infra/monitoring/governance/secret_scan_patterns_v1.json`, TruffleHog/GitLeaks YERİNE geçmez, yazım anında repodaki TÜM izlenen dosyalara karşı sıfır yanlış-pozitifle doğrulandı) ile gerçek bir kontroldür; `classify_contract_changed` artık `scripts/ops/classify_contract_core.py` (AST-tabanlı — `services/tr-en-bridge/src/ollama_nlu.py::classify()`'nin YALNIZCA arayüz yüzeyini çıkarır, docstring/yorum/varsayılan-değer değişikliklerinden ETKİLENMEZ, `infra/contracts/classify_contract_checksum_v1.txt` baseline'ına karşı sha256 checksum karşılaştırması) ile gerçek bir kontroldür — ikisi de önceden hiçbir zaman tetiklenmeyen `pass` yer tutucuydu. **Gerçek Jira legitimacy sağlayıcısı (verify-only):** `scripts/ops/legitimacy_provider_client.py` — `check_ticket_via_jira()`, kimlik bilgilerini YALNIZCA `JIRA_BASE_URL`/`JIRA_EMAIL`/`JIRA_API_TOKEN` ortam değişkenlerinden okur (hiçbir dosyaya yazılmaz), yapılandırılmamışsa GERÇEK ağ çağrısı hiç denemez (`checked=False`), yapılandırılmışsa salt-okunur tek bir `GET /rest/api/2/issue/{ticket_id}` (Basic auth, zaman aşımı+sınırlı yeniden deneme YALNIZCA geçici hatalarda — 404/401/403 ASLA yeniden denenmez), tüm hata yolları redaksiyondan geçer. `emergency_legitimacy_core.py::evaluate_legitimacy`'ye yeni `precomputed_provider_result` parametresi eklendi; `provider='jira'` bu parametre OLMADAN çağrılırsa `ValueError` fırlatır — STUB fonksiyonun (`run_provider_check`) yanlışlıkla gerçek bir Jira sonucunu FABRİKE etmesini önleyen açık bir kod-inceleme-zamanlı güvenlik korumasıyla birlikte (aynı fonksiyon `provider='jira'` için de artık `checked=False`+açıklayıcı mesajla erken döner). `check_emergency_legitimacy.py --provider jira` gerçek uçtan uca kablolandı; `legitimacy_report.json`/`.md` artık `provider_evidence` bloğu içerir. Sonuç: `provider_is_stub_only` blocker'ı artık EK KOD DEĞİŞİKLİĞİ GEREKMEDEN gerçekten temizlenebilir (blocker zaten `evidence_payloads`'taki `provider` alanını `{mock,jira_stub,none}` dışı bir değer için "gerçek" sayıyordu). **Bilinen sınırlama:** jira yapılandırılmamışken (env eksik) sonuç `PASS` döner (provider=none ile AYNI "atlama, başarısızlık değil" semantiği) — gerçek doğrulamanın olup olmadığı yalnızca `provider_evidence.checked` alanından anlaşılır, yalnızca `legitimacy_status=PASS` görmek yeterli değildir. Commit AA/AB testleri: `test_pilot_fpr_core.py`(22)+`test_compute_pilot_false_positive_rate.py`(7)+`test_export_weekly_observability_json.py`(13)+`test_classify_contract_core.py`(12)+`test_secret_scan_core.py`(15)+`test_legitimacy_provider_client.py`(14)+`evaluate_pilot_promotion.py`/`emergency_legitimacy_core.py`/`check_emergency_legitimacy.py`'ye eklenen testler — tam regresyon (Bash+PowerShell) yeşil. Ayrıntı: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Evidence maturity model", "How FPR is computed", "Promotion blockers now auto-checked", "Jira legitimacy verification setup (env-only secrets)". **2026-08-13 Commit AB sirasinda kesfedilen ve duzeltilen kendine-referansli yanlis-pozitif (secrets_committed):** Commit AB testleri commit'e hazirlanirken, `evaluate_pilot_promotion.py --repo-root .` GERCEK repo uzerinde calistirilarak dogrulandi ve `secrets_committed` blocker'inin TUM ozellikleri REJECT'e zorladigi kesfedildi — kok neden, `secret_scan_core.py`'nin KENDI testlerinin (`tests/test_secret_scan_core.py`, `tests/test_evaluate_pilot_promotion.py`) tarayicinin GERCEK deseni yakaladigini kanitlamak icin BILEREK icerdigi sahte-ama-desene-uyan degerlerdi (ornegin `"AKIAABCDEFGHIJKLMNOP"`) — bu iki dosyanin KENDISI git-tracked oldugu icin repo taramasi kendi test fixture'larini "sizmis sir" olarak yanlislikla isaretliyordu. Karar: genel bir `tests/` allowlist'i EKLENMEDI (bu, test dosyalarina yanlislikla commit edilmis GERCEK sirlari da korlestirir, guvenlik amacini bosa cikarirdi) — bunun yerine `infra/monitoring/governance/secret_scan_patterns_v1.json::allowlist_paths`'e YALNIZCA bu iki dosyanin TAM yolu eklendi (dosya-bazli, dizin-bazli DEGIL), gerekce JSON icindeki `$allowlist_paths_note` alaninda belgelendi. Duzeltme sonrasi gercek repo taramasi 0 bulguya dondu (`test_scan_repo_for_secrets_real_repo_and_real_config_finds_nothing` ile kalici regresyon testi eklendi); evaluator gercek calistirmada beklenen duruma geri dondu: `emergency_chain_matching`/`auto_rollback_on_verify_fail` EXTEND_PILOT, `emergency_legitimacy_required` yalnizca `provider_is_stub_only` nedeniyle REJECT. **2026-08-13 Promotion-candidate sprinti (Commit AC/AD):** Kanit olgunlastirma sprinti bir bilinen sinirlama birakmisti -- `provider='jira'` yapilandirilmamiskken sonuc sessizce PASS donuyordu ("hic kontrol edilmedi" ile "gercekten dogrulandi" ayni gorunuyordu). Bu sprint bunu ve iki iliskili boslugu kapatti: (1) `emergency_legitimacy_core.py::evaluate_legitimacy`, `provider='jira'` + `checked=false` icin artik HER ZAMAN `SKIPPED` doner, ASLA `PASS` degil ("no implicit pass on unchecked provider"); `none`/`mock`/`jira_stub` davranisi GERIYE UYUMLU degismedi. (2) `evaluate_pilot_promotion.py`, `emergency_legitimacy_required` icin SKIPPED-durumlu kaniti artik `runs`/`observation_days`/`false_positive_rate` hesabina KATMIYOR (`EvidenceSummary.skipped_evidence_count` ile izlenebilir) VE `provider_is_stub_only` blocker'i artik `provider_evidence.checked=true` ZORUNLU kiliyor (onceden salt `provider="jira"` string'i, checked=false olsa bile, bu blocker'i yanlislikla temizleyebilirdi -- GERCEK bir boslukTU, bu sprintte KAPATILDI). (3) `pilot_promotion_core.py`'ye `validate_fpr_summary_schema`/`validate_weekly_review_schema`/`validate_legitimacy_report_schema` eklendi -- semasi bozuk bir kanit dosyasi yeni `invalid_evidence_schema` blocker'ini tetikler (`pilot_promotion_criteria_v1.json`'da her uc ozellik icin kayitli; `secrets_committed`/`classify_contract_changed` notlarindaki eski "yapisal yer tutucu" ifadesi de bu sirada GUNCELLENDI, cunku onceki sprintte zaten gercek kontrollere donusmuslerdi ama manifest notlari GUNCELLENMEMISTI). (4) `evaluate_pilot_promotion.py --rehearsal` (Commit AC'ye dahil edildi -- gorev planinda AD altinda ongorulmustu, ama evaluator dosyasinin ayni fonksiyonlarinda schema/SKIPPED degisiklikleriyle ic ice gelisti, `git add -p` kullanilamadi): ayni karari hesaplar, "kac calistirma/gun daha gerekli"yi somutlastirir, `pilot_flags_state.json`'a DOKUNMAZ, `rehearsal_report.md`/`.json` yazar. (5) `scripts/ops/run_promotion_evidence_pack.ps1` + `promotion_evidence_pack_core.py` + `build_promotion_runpack_index.py` (Commit AD, yeni) -- chain trial + legitimacy (mock+jira) + FPR + haftalik export + degerlendirici + sonuc birlestirme TEK bir tekrar-uretilebilir komutla; `auto_rollback_evidence_scan` adimi BILEREK GOZLEMSELDIR (gercek bir VerifyReload FAIL/auto-rollback senaryosunu TETIKLEMEZ -- durum-degistiren bir islem olurdu, "Default runtime behavior remains conservative" kisitiyla CELISIRDI; bunun yerine mevcut `apply_report.json` kanitlarini ON/OFF sayar). **Gercek calistirmada iki hata bulunup duzeltildi:** (a) Windows PowerShell 5.1'in `Set-Content -Encoding utf8`'i DAIMA UTF-8 BOM ekliyor, Python `json.loads` bunu reddediyordu (duzeltme: `utf-8-sig` ile okuma + regresyon testi); (b) bir PowerShell fonksiyonunun (`Invoke-PackStep`) `Write-Output` cagrilari donus degerine YANLISLIKLA KARISIYORDU (fonksiyon ciktisi `return` ile SINIRLI DEGILDIR) -- `Write-Host`'a tasindi. Hem `-SkipJira` hem varsayilan (jira ortami yokken kendiliginden SKIPPED) yollari GERCEKTEN calistirilip runpack ciktisi elle dogrulandi. Testler: `test_promotion_evidence_pack_core.py`(20)+`test_build_promotion_runpack_index.py`(7, BOM regresyonu dahil)+`pilot_promotion_core.py`/`evaluate_pilot_promotion.py`/`emergency_legitimacy_core.py`/`check_emergency_legitimacy.py`'ye eklenen ~35 test -- tam regresyon (Bash+PowerShell) yesil. Ayrinti: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Legitimacy status semantics (PASS/FAIL/SKIPPED)", "Promotion evidence runpack usage", "Rehearsal mode interpretation". **2026-08-13/14 Final promotion attempt preparation (Commit AE/AF):** Uc kalan kanit boslugunu kapatmaya calisti -- ikisi kapatildi, biri gercek altyapi eksikligi nedeniyle DURUSTCE kapatilamadi. **On-arastirma (yeni bir seyi calistirmadan ONCE):** ortam kontrol edildi -- JIRA_BASE_URL/JIRA_EMAIL/JIRA_API_TOKEN hicbiri set degil, promtool/amtool bu ortamda kurulu DEGIL. Kullaniciya 3 net soru soruldu (Jira kaniti/auto-rollback drill/adjudikasyon doldurma nasil ilerlensin) -- fabrikasyon riski tasiyan hicbir adim onay ALINMADAN atilmadi. **Kullanici karari:** (1) Jira icin durustce not_collected raporlansin, (2) mevcut GERCEK drill kaniti (bugun daha erken, 18:04:44Z, gercek amtool.exe ile uretilmis) seffaf referanslansin -- yeniden indirme/taze calistirma YAPILMASIN, (3) adjudikasyon YALNIZCA docs/BACKLOG.md'de/pilot_summary.md'de ZATEN yazili sonuclari kaydetsin, YENI bir yargi URETILMESIN. **Commit AE:** `scripts/ops/check_pilot_observation_window.py` (yeni) -- her ozellik icin observed_days/run_count/remaining_days/remaining_runs hesaplar, `evaluate_pilot_promotion.py::compute_evidence_summary`'yi (GERCEK kanit tarama mantigi) yeniden kullanir (tek dogruluk kaynagi, ayri/tutarsiz bir ikinci hesaplama YAZILMADI); `pilot_promotion_core.py::compute_observation_window`/render/write eklendi; `evaluate_pilot_promotion.py` (normal VE --rehearsal) artik EN SON boyle bir raporu bulursa bilgi amacli referans veriyor/kopyaliyor (`observation_window_context.json`) -- karar mantigina KARISTIRILMAZ (cift-kaynak senkron riski YOK, evaluator kendi hesabini bagimsiz yapiyor). **Commit AF:** (a) `infra/monitoring/governance/pilot_fpr_adjudications.json` -- 3 sinyal (`HIGH_NULL_INTENT_RATE-20260810T000000+0000` chain-matching, `HIGH_NULL_INTENT_RATE-20260813T180435` auto-rollback, `TICKET-BAD-FORMAT` legitimacy) icin CONFIRMED_TRUE_POSITIVE kaydedildi -- ucu de `reports/v1_2_pilot_20260813T180035Z/pilot_summary.md`'deki dogrudan alintilarla gerekcelendirildi (`adjudicated_by` kaynak dosya yolunu gosterir); `pilot_fpr_core.py::validate_adjudication_traceability` (yeni) -- hayalet/anonim adjudikasyonlari MEKANIK olarak yakalar, `compute_pilot_false_positive_rate.py`'ye uyari olarak kablolandi. Sonuc: 3 sinyal < MIN_ADJUDICATED_FOR_RATE(3) oldugu icin FPR hala INSUFFICIENT_DATA -- beklenen/durust, fabrike edilmis bir yeterlilik YOK. (b) `scripts/ops/package_promotion_drill.py` + `promotion_drill_core.py` (yeni) -- bir `apply_report.json`'daki VerifyReload FAIL + auto-rollback kanitini (verification_state=FAIL, auto_rollback.triggered+restored=true, checksum eslesmesi: old_checksum==restored_checksum) dogrular, `data/audit/audit.log.jsonl`'deki eslesen `auto_rollback_triggered` kaydini bulur, `reports/promotion_drill_<UTC>/`'a paketler (drill_evidence.json+audit_snippet.json+checksum_proof.json+drill_summary.md/json). **GERCEK bir drill KENDISI TETIKLENMEDI** -- bunun yerine, `reports/v1_2_pilot_20260813T180035Z/scenario2_verify_fail_autorollback/autorollback_ON_strict/apply_report.json`'a (Commit W/X'ten, GERCEK amtool.exe ile, bilinen ${VAR} sinirlamasi tetiklenerek uretilmis) SEFFAF referans verildi -- `is_fresh_run=false` acikca isaretlendi, `drill_summary.md`'ye otomatik bir SEFFAFLIK NOTU eklendi. Kabul: TUM kriterler saglandi (verification_state=FAIL, triggered=true, restored=true, checksum eslesti, audit'te eslesen kayit BULUNDU) -- `reports/promotion_drill_20260813T205030Z/` `git add -f` ile arsivlendi (routine/tekrar-uretilebilir gelecek calistirmalar icin `reports/promotion_drill_*/` .gitignore'a eklendi, ayni `gate_d_real_validation_*` deseni). (c) **Final rehearsal + normal karar:** `run_promotion_evidence_pack.ps1 -SkipJira` + `evaluate_pilot_promotion.py --rehearsal` + normal mod SIRAYLA calistirildi -- karar siniflari BIREBIR eslesti (`emergency_chain_matching`/`auto_rollback_on_verify_fail`: EXTEND_PILOT, `emergency_legitimacy_required`: REJECT [`provider_is_stub_only`], exit code 2/2 ikisinde de). **Genel sonuc: REJECT** -- `emergency_legitimacy_required` GERCEK bir `provider=jira`+`checked=true` kaniti olmadan KOSULSUZ REJECT veriyor; diger iki ozellik gozlem penceresi/calistirma sayisi henuz karsilanmadigindan EXTEND_PILOT (bir SURE meselesi, arac eksikligi DEGIL). Testler: `test_pilot_promotion_core.py`(+7 observation window)+`test_check_pilot_observation_window.py`(6, yeni)+`test_evaluate_pilot_promotion.py`(+5 observation window referansi)+`test_pilot_fpr_core.py`(+6 traceability)+`test_promotion_drill_core.py`(18, yeni)+`test_package_promotion_drill.py`(6, yeni) -- tam regresyon (Bash+PowerShell) yesil. Ayrinti: `docs/ops/MONITORING_STACK_RUNBOOK.md` "Final promotion attempt checklist", "Controlled fail-path drill SOP", "Adjudication traceability rules". |
| B037 | `tools/cli-runner/src/runner.py` PowerShell PATH'inde `echo` çalıştırılabilir dosyasını bulamıyor | Orta | **Kapalı** | Backlog | — | `shutil.which("echo")`, Git Bash'in PATH'inde gerçek bir `echo.exe` bulur ama saf bir PowerShell sürecinin PATH'inde `echo` yalnızca bir dil anahtar kelimesi/alias'tır, gerçek bir yürütülebilir DEĞİLDİR — bu yüzden `test_nlu_provider_flag.py`'nin 3 orchestrator-smoke alt-testi PowerShell üzerinden çalıştırılınca `EXECUTABLE_NOT_FOUND` ile başarısız olurdu (Bash üzerinden sorunsuz geçerdi). 2026-08-13'te `scripts/ops/run_observability_gates.ps1`'in Gate E'sini gerçek PowerShell'de çalıştırırken keşfedildi. Classify/fallback SÖZLEŞMESİNDE bir regresyon DEĞİLDİ — yalnızca `echo_runner.py`'nin CLI aracı keşif mekanizmasının Windows/PowerShell-özel bir PATH sınırlaması. **2026-08-13 DÜZELTİLDİ (Commit O):** `runner.py`'ye, `shutil.which()` başarısız olduğunda küçük/sabit bir komut kümesi (`echo`, `pwd`) için SAF PYTHON eşdeğerleri kullanan bir fallback eklendi (`cmd.exe /c` gibi bir kabuğa fallback YAPILMADI — bu, "shell=True asla kullanılmaz" güvenlik ilkesini dolaylı olarak ihlal ederdi; saf-Python yol subprocess/shell yüzeyi olmadığından harici bir yürütülebilirden DAHA AZ değil DAHA GÜVENLİDİR). Bilinmeyen komutlar için eski davranış (açık `EXECUTABLE_NOT_FOUND` hatası) aynen korundu. 7 yeni parite testi eklendi (`tests/test_cli_runner.py`) — `shutil.which` hem başarılı hem başarısız senaryoları mock'lanarak, gerçek makine PATH durumundan bağımsız deterministik doğrulandı. Sonuç: tam suite (278 test) hem Git Bash hem native PowerShell'de **278/278 yeşil** (önceden PowerShell'de 3 test başarısızdı). |
| B038 | Ops Suite frontend v1 — tam animasyonlu 2D ofis sahnesi/ajan avatarları | Orta | **Kısmen tamamlandı** | Faz 5 | — | 2026-08-14 (T31-T36, ADR-020): saf Canvas2D ofis sahnesi (`scene.js`) GERÇEKTEN uygulandı ve gerçek bir tarayıcıda kanıtlandı (`reports/ops_suite_scene_2026-08-14T0014Z/`) — masa/dinlenme/hayalet-raf düzeni, durum renkleri, hareket enterpolasyonu, asistan "rapor modu", onay-tepsisi rozeti, WS-tetiklemeli güncellemeler, debug köprüsü. **Hâlâ eksik (v1'e):** gerçek sprite/karakter illüstrasyonları (şu an geometrik şekiller), çoklu-adımlı görev animasyonları, ses efektleri, sahneye tıklama etkileşimi — bkz. `docs/OPS_SUITE_PRODUCT_SPEC.md` §7. |
| B039 | Ops Suite gerçek-tarayıcı E2E görsel doğrulama (Playwright vb.) | Orta | **Kısmen tamamlandı** | Faz 5 | — | 2026-08-14 (T29, ADR-021): önceki "tarayıcı-otomasyon aracı YOK" varsayımı BU ORTAM İÇİN GERÇEKTEN test edildi ve tersine döndü — `apps/ops-suite/e2e/` altında gerçek Playwright altyapısı kuruldu, gerçek Chromium indirildi/çalıştırıldı, 2/2 duman testi PASS (kök sayfa render + WS bağlantısı + sesli komut→onay kuyruğu DOM güncellemesi). **Hâlâ eksik:** B038 (animasyonlu sahne) henüz YAPILMADI, o yüzden onun görsel regresyonu da yok; CI/farklı bir ortamda aynı tarayıcı-indirme adımının tekrar doğrulanması gerekir (bkz. ADR-021 kapsam notu). |
| B040 | Ops Suite gerçek ses/GSM/kamera donanım entegrasyonu | Yüksek | Açık | Faz 3/5 | — | Bu ortamda mikrofon/hoparlör/GSM modem/kamera donanımı YOK. Sesli komutlar v0'da yalnızca MOCKED TR metin girdisiyle test edildi (`VoiceBridge.handle_voice_command(input_tr: str)` — sözleşme, gerçek STT/TTS/GSM/kamera eklendiğinde DEĞİŞMEMELİDİR, bkz. `docs/VOICE_FIRST_INTERACTION_POLICY.md` §3, `docs/GSM_CALL_FLOW.md`). B004 (Whisper STT)/B005 (TTS)/B010 (GSM Gateway)/B012 (Gesture-vision) ile birlikte ele alınmalı. |
| B041 | Ops Suite heartbeat/presence durumunun kalıcılığı (restart-sonrası) | Düşük | **Kapalı (minimal dilim)** | Faz 5+ | — | 2026-08-14 (T39, ADR-022): `presence_store.py::PresenceStore` (JSONL append-only, ADR-009/ADR-016 deseni) + sunucu başlangıcında `HeartbeatTracker.has_record()`'ı OLMAYAN ajanlar için tohumlama eklendi — gerçek bir "restart" simülasyonuyla (`tests/test_ops_suite_api.py::test_agent_presence_survives_simulated_restart`) doğrulandı. **Kalan sınırlamalar (v0.1, dürüstçe):** dosya rotasyon/budama YOK; yalnızca `HeartbeatTracker` kalıcı, `AssistantPresenceTracker` DEĞİL — bkz. ADR-022. |
| B042 | Ops Suite frontend için npm/bundler/framework toolchain kararı | Düşük | **Kapalı** | Faz 5+ | — | 2026-08-14 (T29): karar verildi — bkz. `docs/DECISIONS.md` ADR-020. Sunulan uygulama saf HTML/CSS/vanilla-JS + Canvas2D olarak KALIYOR (framework/bundler EKLENMİYOR); PixiJS değerlendirilip bu ölçek için gereksiz karmaşıklık olduğu gerekçesiyle reddedildi. |
| B043 | `GSM_CALL_FLOW.md`'nin gerçek GSM donanımına karşı doğrulanması | Orta | Açık | Faz 3 | Device Agent | `docs/GSM_CALL_FLOW.md` şu an TASARIM-ONLY (2026-08-14) — `services/gsm-gateway/` hâlâ yalnızca `.gitkeep` içeriyor (bkz. B010). Gerçek bir GSM modem/SIM edinildiğinde, belgedeki akışın gerçek donanıma karşı doğrulanması gerekiyor. |
| B044 | Approval/Auth Enforcement + Owner Root Guard (Serkan Eryılmaz root authority) | **Yüksek (SECURITY P0)** | **Kapalı** | Faz 5 (öne çekildi) | — | `IDENTITY_AND_DELEGATION_POLICY.md` §4'ün bilinen v0 sınırlaması (`actor` alanı serbest metin, kimlik doğrulama YOK) kapatıldı — bkz. `docs/DECISIONS.md` ADR-019, `docs/PLAN.md` T28. Bearer-token kimlik doğrulama + kapsam (scope) tabanlı yetkilendirme + owner-only root guard (`risk_level="irreversible"` onayları YALNIZCA sahibi) + genişletilmiş audit alanları (`actor_id`/`auth_method`/`authority_source`/`decision_scope`) eklendi. |
| B045 | Ajan/asistan durum geçişlerini async eventing ile gözlemlenebilir kılmak | Orta | **Kapalı** | Faz 5 | — | 2026-08-14 (T37): `working` gibi kısa ömürlü ara durumlar `GET /api/agents` polling'i ile YAKALANAMIYORDU (senkron çağrı modeli, bkz. eski T33 notu). `heartbeat.py::HeartbeatTracker.on_change` (yeni, opsiyonel, GERİYE UYUMLU kanca) + önceden TANIMLI-ama-HİÇ-KULLANILMAMIŞ `TOPIC_AGENT_PRESENCE` konusu artık GERÇEKTEN yayınlanıyor — bkz. `docs/DECISIONS.md`/`docs/PLAN.md` T37, `tests/test_ops_suite_ws.py::test_ws_receives_agent_presence_events_for_working_then_idle`. |
| B046 | `working`→`idle` geçişi için deterministik test kapsamı (frontend tüketimi dahil) | Düşük | **Kapalı** | Faz 5+ | — | 2026-08-14 (T38): `scene.js::applyAgentPresenceEvent()` artık `agent.presence` WS mesajlarını ANINDA tüketiyor (polling beklemeden). `scene.spec.js`'e Playwright'ın native WS frame API'siyle (sleep/`waitForTimeout` YOK) `working`→`idle`'i deterministik doğrulayan yeni bir senaryo eklendi — 6/6 PASS, 3 ardışık kosuda doğrulandı. Süreçte gerçek bir test-izolasyonu hatası (paylaşılan sunucu state'i yüzünden erken-geçen bir assertion) bulunup düzeltildi, bkz. `docs/PLAN.md` T38. |
| B047 | Ofis sahnesi — yerel sprite/karakter varlık hattı (asset pipeline) | Orta | **Kapalı** | Faz 5+ | — | B038'in v0 kapsamı geometrik şekiller (daire + baş harfler) kullanıyordu (bkz. `docs/OPS_SUITE_PRODUCT_SPEC.md` §7 "kapsam dışı"). **Kabul kriteri:** yerel dosya sisteminden (CDN YOK, ADR-018/020 ile tutarlı) yüklenen ajan/asistan başına ayırt edilebilir bir sprite/ikon; varlık eksik/bozuksa MEVCUT geometrik render'a GERİ DÜŞME (sessizce boş bırakma DEĞİL); gerçek bir tarayıcıda görsel olarak doğrulanmalı. **2026-08-14 kapatıldı:** `apps/ops-suite/frontend/assets/sprites/*.svg` (5 yerel dosya) + `scene.js` sprite/geri-düşme render yolu eklendi; gerçek bulunan SVG-yorum XML hatası düzeltildi; kanıt `apps/ops-suite/e2e/tests/interactions.spec.js` (2 test) ve `reports/ops_suite_interactions_2026-08-14T0126Z/`, bkz. `docs/PLAN.md` T40. |
| B048 | Ofis sahnesi — çoklu-adımlı görev animasyonları | Düşük | Açık | Faz 5+ | — | B038 v0'da yalnızca durum-bazlı (working/idle/vb.) konum geçişleri var — "belge taşınıyor", "masalar arası devir" gibi ARA adım animasyonları YOK. **Kabul kriteri:** en az bir çok-adımlı görev senaryosunun (ör. onay bekleyen bir görevin masadan onay-tepsisine "yürümesi") ara karelerle animasyonu; henüz TASARLANMADI, bu satır yalnızca kapsamı kaydeder. |
| B049 | Ofis sahnesi — tıklama etkileşimleri (ajan detay paneli) | Orta | **Kapalı** | Faz 5+ | — | B038 v0 sahnesi salt-görsel (tıklanamaz). **Kabul kriteri:** bir ajana (hayalet raftakiler dahil) tıklamak, o ajanın GERÇEK durumunu (state, last_task_id, last_heartbeat_ts, detail) gösteren bir detay paneli açar; ajan `last_task_id`'si bekleyen bir onay kaydıyla eşleşiyorsa panelde o kayda giden bir bağlantı/vurgulama olur; ajanların KENDİ bir "yetki kapsamı" (authority scope) OLMADIĞI açıkça belirtilir (bu bir insan-kimlik kavramıdır, bkz. IDENTITY_AND_DELEGATION_POLICY.md) — fabrike bir scope UYDURULMAZ. **2026-08-14 kapatıldı:** `scene.js` canvas tıklama hit-test'i + `app.js::openAgentDetailPanel()` + `#agent-detail-panel` DOM'u eklendi; onay bağlantısı `last_task_id === request_id` eşleşmesiyle kuruluyor; kanıt `apps/ops-suite/e2e/tests/interactions.spec.js` (4 test) ve `reports/ops_suite_interactions_2026-08-14T0126Z/`, bkz. `docs/PLAN.md` T42. |
| B050 | Ofis sahnesi — ses efektleri/geri bildirimi | Düşük | Açık | Faz 5+ | — | B038 v0'da hiçbir ses YOK (görev tamamlanma/onay bekleme gibi olaylar için). **Kabul kriteri:** en az durum değişikliklerinde (ör. onay bekleniyor) kısa, yerel (CDN'siz) bir ses ipucu; kullanıcı tercihiyle KAPATILABİLİR olmalı (varsayılan açık/kapalı kararı ayrı verilecek); henüz TASARLANMADI, bu satır yalnızca kapsamı kaydeder. |

## B036 Çıkış Kriterleri (Kilitli Checklist)

> Bu checklist, B036'nın `IN_PROGRESS` → `READY_FOR_RETEST` geçişi için
> **tüm maddeleri birden** sağlaması gereken, kilitlenmiş resmi kriter
> setidir. Hiçbir madde varsayımla/tahminle işaretlenemez — yalnızca
> gerçek, kanıtlanmış bir koşu sonucunda işaretlenebilir. Güncel durum:
> **IN_PROGRESS** (aşağıdaki maddelerin hiçbiri henüz sağlanmadı).

- [ ] Vulkan yolunda (varsayılan profil, workaround YOK) en az **50 ardışık**
  çağrı çökmeden tamamlanmalı
- [ ] Repro scripti (`scripts/repro_ollama_crash.ps1` veya
  `scripts/repro_b036_batch.ps1`), **2 ayrı soğuk restart** arasında
  tutarlı şekilde başarı üretmeli
- [ ] stderr/log paketinde **`0xc0000005` imzası hiç görünmemeli**
- [ ] Kanıt, incident report yolu altında arşivlenmeli
  (`reports/runtime_incident_20260813T004855Z/` veya yeni bir
  `runtime_incident_<timestamp>/` klasörü)
- [ ] `docs/RUNBOOK.md`, zaman damgası ve artifact bağlantılarıyla
  güncellenmeli

Yalnızca bu beş madde de gerçek kanıtla işaretlendiğinde B036
`READY_FOR_RETEST`'e geçebilir; bu noktadan sonra B031 resmi gate'i
yeniden koşulabilir.
